[pytest]
asyncio_mode = auto
addopts = -ra --strict-markers --strict-config --import-mode=importlib
markers =
    integration: requires PostgreSQL or Redis services
    slow: takes longer than five seconds
