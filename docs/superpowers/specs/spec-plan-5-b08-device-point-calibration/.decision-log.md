# Decision Log

- 2026-08-27 — Headless express distillation was used because the device-evidence intent and source evidence were already available. The repository has no `_bmad` customization/config runtime, so the canonical kernel was added alongside the existing `docs/superpowers/specs` artifacts without relocating them.
- 2026-08-27 — Preservation verdict: all load-bearing claims are represented in the kernel or companions, including the unproven SQL table identity, BCMM/CBMM coexistence, CBMM's limited confidence lift, candidate `ValueType=s16` mapping hypothesis, FC1/RBit conflict, legacy formula conflicts, production non-mutation boundary and B-08 prerequisites. Wrapper-only conversational approvals were not copied.
- 2026-08-27 — Coherence verdict: five capabilities each contain intent and testable success; constraints rule out unsafe inference and production mutation; non-goals are explicit; success is demonstrable. The calibration rule was hardened because A/B alone can always fit an affine line: formal resolution now needs independent C holdout plus A' return, or authoritative device/firmware binding plus A/B/A' agreement.
- 2026-08-27 — Adversarial review verdict: the first draft incorrectly marked the story done while all physical identity/calibration questions remained open and the source JSON had no schema-derived eligibility. It also reused B-07, which the canonical deployment contract already assigns to backup/restore. The story is renumbered B-08 and remains blocked. The contract requires a reproducible MDF parser, field-level machine evidence, immutable scoped approval, uncertainty-based A/B/C/A' quality, B-06-grade calibration auditing and a separate approved implementation/canary specification; no production action is authorized here.
- 2026-08-27 — Offline implementation verdict: parser/canonical evidence and point-profile Schema/validator are complete and locally verified. The candidate mapper deliberately leaves line address/8N1 unresolved, separates analog/binary/counter calibration and produces BLOCKED for all current evidence. The formal Plan 5 contract now makes B-08 a non-waivable Gate 0 blocker and reserves B-09 for runtime/onboarding. Coherence pass: all five capability IDs remain stable and their success clauses match executable gates; parser completion cannot close physical work. Preservation pass: source/page/record provenance, field claims, contradictions, approval/runtime hashes, calibration kinds, B-07/B-08 boundary and no-canary authorization are present in the kernel or companions. B-08 remains blocked and no production authority was added.
- 2026-08-27 — Acceptance review correction: the preceding statement that the eligibility Schema/validator was complete is superseded. Parser/candidate preservation remains independently reviewable, but the qualification gate returns to blocked review until external trust verifies four-role signatures, a canonical gate digest binds Schema plus executable policy and evidence contracts, per-point role/subject and line evidence are enforced, analog/binary/counter evidence content is parsed, runtime PASS comes from an attested trusted runner, and runtime target identity comes from a trusted `ReleaseVerificationReceipt` covering the OpenSSH candidate chain and actual image/migration observations. ELIGIBLE remains a qualification result only and creates no authority for TX, rebuild, canary, continuous polling or production cutover; no future digest is recorded before the implementation artifacts exist.
- 2026-08-27 — Trust hardening implementation: the qualification validator now takes trust policy only as an external input, verifies four-role/evidence/runtime signatures and a dedicated OpenSSH-SSHSIG release-verifier receipt, binds all eligibility metadata in a canonical gate digest, parses kind-specific evidence, enforces line and per-point subject binding, and derives runtime identity from that receipt rather than a caller-supplied Manifest. The extractor now binds the repository MDF identity, rejects Windows ADS/reparse output, publishes atomically and independently asserts SQL page/file header fields. Current legacy evidence remains BLOCKED; this implementation does not close physical calibration or B-09 runtime work and grants no execution authority.
- 2026-08-29 — Offline acceptance closeout: the v5 Schema/validator, v3 qualification toolchain and receipt producer are recorded as `complete_offline`, and the historical adversarial findings are closed at the repository layer. This does not change B-08's overall `blocked` status. Manifest v2 remains valid only for general verification and existing remote maintenance; B-08 qualification/receipt, G0-06, G4-01, B-09 and canary require v3 plus a valid receipt. Two external blockers remain explicit: anti-rollback trust-root provisioning with an external high-water tuple `(root_id, root_version, revocation_sequence, root_sha256)` protected by TPM NV/equivalent hardware or a remote freshness witness, and Windows verifier runtime/signing-key isolation with protected runtime/dependency/key identity evidence. No final test count, final artifact digest or现场结果 was asserted by this documentation-only closeout.
- 2026-08-29 — Final offline verification supersedes the preceding no-digest/no-count closeout note: release/archive, remote-audit, validator and extractor inputs are bounded before allocation; parser-source path reopening and receipt/runtime/approval chronology gaps are closed with negative regressions. The regenerated canonical ledger is `8190754...A7447`, extractor source is `852D9326...6BD1`, Schema is `72636AE0...F52D`, validator v5 source is `7A50B4C1...A9A6`, and legacy validation remains exit 2 / BLOCKED / 292. Full Python is 1421 passed/15 skipped and frontend E2E is 20 passed/2 skipped. Loader-byte identity cannot be established by a process rereading mutable source, so it remains part of B-08-W; no target-machine or production authority was added.
- 2026-08-29 — Publisher/runtime review correction: qualification now starts outside the candidate and exposes only `ValidatorSchema`, `ValidatorProfile`, `ValidatorLegacy` and `Receipt`; FC2 requires discrete-input-specific address evidence. Toolchain archives are canonical single-member gzip plus deterministic strict USTAR, OCI overlay/migration inspection has explicit metadata/file/member budgets and strict zero-length regular-file whiteouts, and the Alembic head is derived from the actually loaded API image without executing migration code. Build and receipt share a protected host-level candidate lock, while POSIX process groups and Windows gated kill-on-close Jobs are cleaned on every exit path. These repository controls do not close B-08-T, B-08-W, physical calibration or B-09, so B-08 remains blocked.
- 2026-08-30 — Final gate rerun supersedes earlier digest/count snapshots: after strict pre-commit Ruff/Mypy compatibility corrections, the canonical ledger regenerated from the original MDF as `7F90E12C...121D9` and differed from the preceding ledger only in its bound extractor digest `BDD85C04...DF77`; Schema is `93A23C65...A244F`, validator is `4DB30F05...3644`, release artifacts is `CDEB719F...D11B9`, receipt producer is `7003C9F3...D8354`, and package-external bootstrap is `ABE13226...5005`. Final review added raw PAX/GNU preflight for Docker outer/layer tar streams, duplicate outer-member rejection in receipt migration inspection, receipt/tag preservation after post-publication lock-release failure, content-SHA audit-cache validation, a manifest-inclusive 32,768-file Windows runtime ceiling, and fail-closed Windows routing from Python bootstrap to the protected PowerShell publisher. Legacy remains exit 2 / BLOCKED / 292 with payload `BE7ECBC3...3440`; tools are 799 passed/7 skipped, full Python 1527 passed/15 skipped, and frontend E2E is 20 passed/2 skipped after a test-local 60-second budget stabilized the 13-page navigation sweep. No target or production action occurred, so the external blockers and overall blocked status remain unchanged.
