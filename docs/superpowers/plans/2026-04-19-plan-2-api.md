# Plan 2 — ruisheng-api FastAPI Service Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 `ruisheng-api` FastAPI 后端服务，提供 REST + WebSocket 接口，覆盖认证、设备、点位、告警、控制、组织、报表、波形、计划、组态、支付、通知、调度的 MVP 范围。

**Architecture:** FastAPI + SQLAlchemy 2.0 async + asyncpg + redis-py 5.x。复用 `ruisheng-shared` ORM 26 表 + ErrCode + enums + schemas。认证 JWT + bcrypt + ULID + jti 黑名单 Redis；多租户走 PG RLS (`SET LOCAL app.tenant_id/role`) + ORM 二次过滤；APScheduler 调度；slowapi 限流；Redis Streams 消费告警 + Pub/Sub 转 WS。

**Tech Stack:** Python 3.11 / FastAPI 0.111+ / uvicorn / SQLAlchemy 2.0 asyncio / asyncpg / redis-py 5.x / pydantic 2.7+ / pydantic-settings / loguru / python-jose[cryptography] / passlib[bcrypt] / python-ulid / slowapi / APScheduler 3.x / openpyxl / aiohttp。测试 pytest-asyncio + fakeredis + testcontainers + httpx。

**前置条件：**
- Plan 0 完整闭环 ✅（alembic 26 表已建）
- Plan 1 完整闭环 ✅（`ruisheng-gw` 发布 `gw-v0.1.0`）
- `ruisheng-shared` 已含 26 张表 ORM model + ErrCode + enums + pydantic schemas
- master 分支：`850f09f`；worktree 分支：`feature/plan-0-foundation`

**工作流约定（同 Plan 1）：**
- Worktree 分支 `feature/plan-2-api`（从 `feature/plan-0-foundation` 派生）；代码只改 worktree，不改 master
- 每 Task 跑 `uv run pytest <path>`（不带 `--cov`，CI 才强制）
- pre-commit mypy `additional_dependencies` 每引新三方 lib 同步加
- spec/plan bug → implementer **必须 BLOCKED 上报**，controller 反向 fix master，绝不静默改
- 每 Stage 收尾打 tag `plan-2-stage-X-complete`
- 全部 Stage 完成后打 `plan-2-complete` + `api-v0.1.0` + GitHub Release（CHANGELOG.md 追加 `[api-0.1.0]`）

**Worktree 启动命令（一次性）：**
```bash
cd "D:\江苏润盛"
git worktree add -b feature/plan-2-api .claude/worktrees/plan-2-api feature/plan-0-foundation
cd .claude/worktrees/plan-2-api
export RUISHENG_API_DB_URL='postgresql+asyncpg://ruisheng_api:dev@127.0.0.1:5432/ruisheng'
export RUISHENG_API_GW_DB_URL='postgresql+asyncpg://ruisheng_gw:dev@127.0.0.1:5432/ruisheng'
export RUISHENG_API_REDIS_URL='redis://:dev-redis-pw@127.0.0.1:6379/0'
export RUISHENG_API_JWT_SECRET="$(python -c 'import secrets;print(secrets.token_hex(32))')"
docker compose -f docker-compose.dev.yml ps        # 确认 PG + Redis healthy
uv sync --all-packages                              # 装 ruisheng-api 依赖
uv run pytest ruisheng-api/tests/unit -x           # 冒烟
```

---

## File Structure

新增 `ruisheng-api/` 包。目录结构：

```
ruisheng-api/
├── pyproject.toml
└── src/ruisheng_api/
    ├── __init__.py                     # __version__ = "0.1.0"
    ├── __main__.py                     # python -m ruisheng_api
    ├── main.py                         # FastAPI app factory + lifespan
    ├── config.py                       # pydantic-settings（API_ 前缀）
    ├── logging_setup.py                # loguru + 脱敏 filter
    ├── deps.py                         # DI：db / redis / current_user / rbac
    ├── core/
    │   ├── __init__.py
    │   ├── errors.py                   # BizError → ApiResponse handler
    │   ├── security.py                 # JWT issue/verify + bcrypt + fingerprint
    │   ├── tenant.py                   # SET LOCAL + ORM filter
    │   ├── rbac.py                     # 4 级 + OTP 要求
    │   ├── login_limit.py              # Redis 分布式登录锁
    │   └── response.py                 # ApiResponse model + ok()/fail()
    ├── db/
    │   ├── __init__.py
    │   ├── base.py                     # async engine + session factory
    │   └── repositories/
    │       ├── __init__.py
    │       ├── users.py
    │       ├── devices.py
    │       ├── points.py
    │       ├── alarms.py
    │       ├── control.py
    │       ├── plans.py
    │       ├── scenes.py
    │       └── pay.py
    ├── api/
    │   ├── __init__.py                 # mount_routers()
    │   ├── schemas/                    # request/response pydantic（此服务私有，非 ruisheng-shared）
    │   │   ├── __init__.py
    │   │   ├── auth.py
    │   │   ├── devices.py
    │   │   ├── alarms.py
    │   │   ├── control.py
    │   │   ├── orgs.py
    │   │   ├── plans.py
    │   │   ├── scenes.py
    │   │   ├── reports.py
    │   │   └── ws.py                   # WSMessage 联合类型
    │   ├── auth.py
    │   ├── devices.py
    │   ├── points.py
    │   ├── alarms.py
    │   ├── control.py
    │   ├── orgs.py
    │   ├── plans.py                    # timing + maintenance
    │   ├── scenes.py
    │   ├── reports.py
    │   ├── waveforms.py
    │   ├── pay.py
    │   ├── admin.py                    # log level / meta
    │   ├── ws.py
    │   └── health.py
    ├── services/
    │   ├── __init__.py
    │   ├── notification/
    │   │   ├── __init__.py
    │   │   ├── base.py                 # INotifier Protocol
    │   │   ├── wechat.py
    │   │   ├── sms.py
    │   │   ├── voice.py
    │   │   └── email.py
    │   ├── wechat_oauth.py
    │   ├── wechat_pay.py
    │   ├── otp.py                      # Redis OTP issue/verify
    │   ├── analytics/
    │   │   ├── __init__.py
    │   │   ├── fft.py
    │   │   └── opm.py
    │   └── reports/
    │       ├── __init__.py
    │       ├── aggregator.py
    │       └── excel_export.py
    ├── tasks/                          # APScheduler jobs
    │   ├── __init__.py
    │   ├── scheduler.py                # AsyncIOScheduler factory
    │   ├── token_refresh.py
    │   ├── pay_expire.py
    │   └── vacuum_hot.py
    ├── pubsub/
    │   ├── __init__.py
    │   ├── publisher.py                # XADD stream:control:cmd
    │   ├── alarm_consumer.py           # XREADGROUP stream:alarm:fired
    │   ├── realtime_bridge.py          # Redis Pub/Sub → WS
    │   └── ws_manager.py               # WebSocket 连接管理
    └── health.py                       # /api/health 多维度

ruisheng-api/tests/
├── unit/
│   └── (mirror of src 结构)
└── integration/
    └── (需 PG + Redis 的集成测)
```

**根 `pyproject.toml` 修改**：`[tool.uv.workspace].members` 追加 `"ruisheng-api"`；`[tool.pytest.ini_options].testpaths` 追加 `"ruisheng-api/tests/unit"` + `"ruisheng-api/tests/integration"`；`pythonpath` 追加 `"ruisheng-api/src"`；`[tool.coverage.run].source` 追加 `"ruisheng-api/src"`；`[tool.mypy].mypy_path` 追加 `ruisheng-api/src`。

---

## Stage 导航

| Stage | 内容 | task 数 | 前置 |
|---|---|---|---|
| A | Scaffold：包 / config / DB / errors / health / main | 5 | — |
| B | Security：logging / JWT+bcrypt / tenant / rbac / deps | 5 | A |
| C | Auth API：login / register+SMS / refresh+logout / OTP | 4 | B |
| D | Devices：CRUD / realtime / history / control | 5 | C |
| E | Points + Alarm configs + Alarm records | 3 | D |
| F | Redis Streams + WS：alarm consumer / WS manager / WS endpoint | 4 | E |
| G | Organizations：wx_groups / users CRUD / 通讯录 | 3 | F |
| H | Reports + Waveforms：日报 / Excel / 波形 + FFT | 3 | G |
| I | Plans + Scenes：timing / maintenance / scene_pages / scene_views | 3 | H |
| J | Notifications：INotifier / wechat / sms / email + fan-out | 3 | I |
| K | APScheduler：token refresh / pay_expire / vacuum | 3 | J |
| L | WeChat Pay：下单 / 回调验签 / 幂等 | 2 | K |
| M | Admin + CI + tag + Release | 3 | L |

**合计 ≈ 46 task / 13 Stage。**

---

## Stage A: Scaffold

### Task A1: 新建 ruisheng-api 包 + 注册 workspace

**Files:**
- Create: `ruisheng-api/pyproject.toml`
- Create: `ruisheng-api/src/ruisheng_api/__init__.py`
- Create: `ruisheng-api/src/ruisheng_api/__main__.py`
- Create: `ruisheng-api/src/ruisheng_api/py.typed` (空文件)
- Modify: `pyproject.toml`（根）：`members` / `testpaths` / `pythonpath` / `coverage.source` / `mypy_path`
- Test: `ruisheng-api/tests/unit/test_smoke.py`

- [ ] **Step 1：写 smoke 测试**

`ruisheng-api/tests/unit/test_smoke.py`:
```python
def test_package_importable():
    import ruisheng_api

    assert ruisheng_api.__version__ == "0.1.0"
```

- [ ] **Step 2：跑 → FAIL（ModuleNotFoundError）**

`uv run pytest ruisheng-api/tests/unit/test_smoke.py -x`

- [ ] **Step 3：新建 ruisheng-api/pyproject.toml**

```toml
[project]
name = "ruisheng-api"
version = "0.1.0"
description = "江苏润盛 IoT SCADA Web API 服务"
requires-python = ">=3.11,<3.12"
license = { text = "Proprietary" }
dependencies = [
  "ruisheng-shared",
  "fastapi>=0.111",
  "uvicorn[standard]>=0.30",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.29",
  "redis[hiredis]>=5.0",
  "pydantic>=2.7",
  "pydantic-settings>=2.3",
  "loguru>=0.7",
  "python-jose[cryptography]>=3.3",
  "passlib[bcrypt]>=1.7",
  "python-ulid>=2.0",
  "slowapi>=0.1.9",
  "apscheduler>=3.10",
  "openpyxl>=3.1",
  "aiohttp>=3.9",
  "httpx>=0.27",
]

[tool.uv.sources]
ruisheng-shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ruisheng_api"]
```

- [ ] **Step 4：新建 src 骨架**

`ruisheng-api/src/ruisheng_api/__init__.py`:
```python
"""ruisheng-api：FastAPI Web 服务。"""

__version__ = "0.1.0"
```

`ruisheng-api/src/ruisheng_api/__main__.py`:
```python
"""`python -m ruisheng_api` 入口（Stage A5 后接 uvicorn）。"""

from __future__ import annotations


def main() -> None:
    raise SystemExit("Stage A5 introduces FastAPI app; not ready yet")


if __name__ == "__main__":
    main()
```

`ruisheng-api/src/ruisheng_api/py.typed` 创建为空文件。

- [ ] **Step 5：修改根 pyproject.toml**

添加到 `[tool.uv.workspace].members`：`"ruisheng-api"`（作为第 4 个 member）。

根 pyproject.toml：
- `[tool.pytest.ini_options].testpaths` 追加 `"ruisheng-api/tests/unit"` 和 `"ruisheng-api/tests/integration"`
- `[tool.pytest.ini_options].pythonpath` 追加 `"ruisheng-api/src"`
- `[tool.coverage.run].source` 追加 `"ruisheng-api/src"`
- `[tool.mypy].mypy_path` 追加 `:ruisheng-api/src`（冒号分隔）
- `[tool.mypy].exclude` 追加 `"ruisheng-api/tests/.*"`

- [ ] **Step 6：`uv sync --all-packages`**

`uv sync --all-packages`；预期：ruisheng-api 入 editable install。

- [ ] **Step 7：跑 smoke → PASS**

`uv run pytest ruisheng-api/tests/unit/test_smoke.py -x`

- [ ] **Step 8：commit**

```bash
git add ruisheng-api pyproject.toml uv.lock
git commit -m "feat(api): A1 scaffold ruisheng-api package + uv workspace registration

- new ruisheng-api/ package under uv workspace
- deps: fastapi, sqlalchemy async, asyncpg, redis, loguru, python-jose, passlib, ulid, slowapi, apscheduler, openpyxl, aiohttp
- root pyproject testpaths/pythonpath/coverage.source/mypy_path updated
- smoke test: import ruisheng_api.__version__ == 0.1.0"
```

### Task A2: Config (pydantic-settings)

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/config.py`
- Test: `ruisheng-api/tests/unit/test_config.py`

- [ ] **Step 1：写 4 个测试**

```python
# ruisheng-api/tests/unit/test_config.py
import pytest
from pydantic import ValidationError

from ruisheng_api.config import Config


def _env(monkeypatch, **overrides):
    base = {
        "API_DB_URL": "postgresql+asyncpg://u:p@h/d",
        "API_GW_DB_URL": "postgresql+asyncpg://u:p@h/d",
        "API_REDIS_URL": "redis://:p@h/0",
        "API_JWT_SECRET": "x" * 64,
    }
    for k in list(base):
        monkeypatch.delenv(k, raising=False)
    for k, v in {**base, **overrides}.items():
        monkeypatch.setenv(k, v)


def test_config_happy(monkeypatch):
    _env(monkeypatch)
    c = Config()
    assert c.db_url.startswith("postgresql+asyncpg")
    assert c.listen_port == 8000
    assert c.jwt_access_ttl_sec == 900


def test_config_missing_required(monkeypatch):
    _env(monkeypatch)
    monkeypatch.delenv("API_JWT_SECRET")
    with pytest.raises(ValidationError):
        Config()


def test_config_extra_forbid(monkeypatch):
    _env(monkeypatch, API_UNKNOWN="x")
    with pytest.raises(ValueError, match="extra unknown API_"):
        Config()


def test_config_jwt_secret_too_short(monkeypatch):
    _env(monkeypatch, API_JWT_SECRET="short")
    with pytest.raises(ValidationError):
        Config()
```

- [ ] **Step 2：跑 → FAIL（无 Config 类）**

- [ ] **Step 3：实现 config.py**

```python
"""api 服务配置。API_ 前缀环境变量；启动时严格校验。"""

from __future__ import annotations

import os
from typing import Any

from pydantic import Field, model_validator
from pydantic_settings import BaseSettings, SettingsConfigDict


class Config(BaseSettings):
    model_config = SettingsConfigDict(
        env_prefix="API_",
        extra="forbid",
        case_sensitive=False,
    )

    listen_host: str = Field(default="127.0.0.1")
    listen_port: int = Field(default=8000, ge=1, le=65535)

    db_url: str = Field(..., description="ruisheng_api role async URL")
    gw_db_url: str = Field(..., description="ruisheng_gw BYPASSRLS role for wxpay callback")
    redis_url: str = Field(..., description="redis-py URL")

    jwt_secret: str = Field(..., min_length=32)
    jwt_access_ttl_sec: int = Field(default=900, ge=60)
    jwt_refresh_ttl_sec: int = Field(default=7 * 24 * 3600, ge=3600)
    otp_ttl_sec: int = Field(default=300, ge=60)

    db_pool_size: int = Field(default=20, ge=1)
    db_pool_overflow: int = Field(default=10, ge=0)

    login_fail_user_max: int = Field(default=5, ge=1)
    login_fail_user_window_sec: int = Field(default=300, ge=30)
    login_lock_ttl_sec: int = Field(default=1800, ge=60)
    login_fail_ip_max: int = Field(default=20, ge=1)
    ip_block_ttl_sec: int = Field(default=3600, ge=60)

    slowapi_rate_default: str = Field(default="100/minute")
    slowapi_rate_login: str = Field(default="5/minute")

    env: str = Field(default="dev")  # dev / test / prod

    @model_validator(mode="before")
    @classmethod
    def _reject_unknown_api_env(cls, values: Any) -> Any:
        known = {name.upper() for name in cls.model_fields}
        unknown = sorted(
            k for k in os.environ if k.startswith("API_") and k[4:].upper() not in known
        )
        if unknown:
            raise ValueError(f"extra unknown API_ env vars: {unknown}")
        return values
```

- [ ] **Step 4：跑 → PASS**

- [ ] **Step 5：commit**

```bash
git add ruisheng-api/src/ruisheng_api/config.py ruisheng-api/tests/unit/test_config.py
git commit -m "feat(api): A2 Config pydantic-settings + extra=forbid env guard

- API_ prefix; required: db_url/gw_db_url/redis_url/jwt_secret(min 32)
- defaults for ports, JWT TTL, pool, login limiter, slowapi rates
- model_validator rejects unknown API_* env (mirrors gw D7 defense)"
```

### Task A3: DB async base + session factory

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/db/__init__.py`
- Create: `ruisheng-api/src/ruisheng_api/db/base.py`
- Test: `ruisheng-api/tests/unit/db/test_base.py`

- [ ] **Step 1：写测试**

```python
# ruisheng-api/tests/unit/db/test_base.py
import pytest
from sqlalchemy.ext.asyncio import AsyncEngine, AsyncSession, async_sessionmaker

from ruisheng_api.db.base import build_engine, build_session_factory


def test_build_engine_returns_async_engine():
    eng = build_engine("postgresql+asyncpg://u:p@h/d", pool_size=5, max_overflow=2)
    assert isinstance(eng, AsyncEngine)


def test_build_session_factory():
    eng = build_engine("postgresql+asyncpg://u:p@h/d", pool_size=5, max_overflow=2)
    factory = build_session_factory(eng)
    assert isinstance(factory, async_sessionmaker)


def test_build_engine_rejects_sync_url():
    with pytest.raises(ValueError, match="async URL required"):
        build_engine("postgresql://u:p@h/d", pool_size=5, max_overflow=2)
```

- [ ] **Step 2：跑 → FAIL**

- [ ] **Step 3：实现 db/base.py**

```python
"""SQLAlchemy 2.0 async engine + Session factory。"""

from __future__ import annotations

from sqlalchemy.ext.asyncio import (
    AsyncEngine,
    AsyncSession,
    async_sessionmaker,
    create_async_engine,
)


def build_engine(url: str, *, pool_size: int, max_overflow: int) -> AsyncEngine:
    if "+asyncpg" not in url and "+aiosqlite" not in url:
        raise ValueError("async URL required (e.g. postgresql+asyncpg://...)")
    return create_async_engine(
        url,
        pool_size=pool_size,
        max_overflow=max_overflow,
        pool_pre_ping=True,
        future=True,
    )


def build_session_factory(engine: AsyncEngine) -> async_sessionmaker[AsyncSession]:
    return async_sessionmaker(
        engine, class_=AsyncSession, expire_on_commit=False, autoflush=False
    )
```

`ruisheng-api/src/ruisheng_api/db/__init__.py`:
```python
"""DB 访问层。"""
from .base import build_engine, build_session_factory

__all__ = ["build_engine", "build_session_factory"]
```

- [ ] **Step 4：跑 → PASS**

- [ ] **Step 5：commit**

```bash
git add ruisheng-api/src/ruisheng_api/db ruisheng-api/tests/unit/db
git commit -m "feat(api): A3 DB async engine + session factory

- build_engine: rejects sync URLs, pool_pre_ping=True
- build_session_factory: expire_on_commit=False (async pattern)"
```

### Task A4: core/errors.py + core/response.py + 统一异常 handler

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/core/__init__.py`
- Create: `ruisheng-api/src/ruisheng_api/core/response.py`
- Create: `ruisheng-api/src/ruisheng_api/core/errors.py`
- Test: `ruisheng-api/tests/unit/core/test_errors.py`

- [ ] **Step 1：写测试**

```python
# ruisheng-api/tests/unit/core/test_errors.py
import pytest
from fastapi import FastAPI, Request
from fastapi.testclient import TestClient
from pydantic import ValidationError

from ruisheng_api.core.errors import register_exception_handlers
from ruisheng_api.core.response import ApiResponse, fail, ok
from ruisheng_shared.errors.codes import BizError, ErrCode


def test_ok_shape():
    r = ok(data={"x": 1})
    assert r.code == 0
    assert r.msg == "ok"
    assert r.data == {"x": 1}


def test_fail_shape():
    r = fail(ErrCode.BAD_PARAM, "bad")
    assert r.code == -100
    assert r.msg == "bad"
    assert r.data is None


def test_biz_error_handler_maps_http():
    app = FastAPI()
    register_exception_handlers(app)

    @app.get("/boom")
    async def boom():
        raise BizError(ErrCode.DEV_OFFLINE, "offline")

    r = TestClient(app).get("/boom")
    assert r.status_code == 200  # ErrCode 映射
    body = r.json()
    assert body["code"] == -200
    assert body["msg"] == "offline"


def test_generic_500_returns_api_response():
    app = FastAPI()
    register_exception_handlers(app)

    @app.get("/boom")
    async def boom():
        raise RuntimeError("oops")

    r = TestClient(app).get("/boom")
    assert r.status_code == 500
    assert r.json()["code"] == -300


def test_validation_error_maps_to_bad_param():
    app = FastAPI()
    register_exception_handlers(app)
    from pydantic import BaseModel

    class Body(BaseModel):
        x: int

    @app.post("/v")
    async def v(body: Body):
        return ok()

    r = TestClient(app).post("/v", json={"x": "nope"})
    assert r.status_code == 400
    assert r.json()["code"] == -100
```

- [ ] **Step 2：跑 → FAIL**

- [ ] **Step 3：实现 core/response.py**

```python
"""ApiResponse 统一包装（对应 spec §5.1）。"""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ruisheng_shared.errors.codes import ErrCode


class ApiResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")

    code: int
    msg: str = "ok"
    data: Any = None
    transid: str | None = None


def ok(data: Any = None, transid: str | None = None) -> ApiResponse:
    return ApiResponse(code=ErrCode.OK.value, msg="ok", data=data, transid=transid)


def fail(code: ErrCode, msg: str, transid: str | None = None) -> ApiResponse:
    return ApiResponse(code=code.value, msg=msg, data=None, transid=transid)
```

- [ ] **Step 4：实现 core/errors.py**

```python
"""统一异常注册器。将 BizError / ValidationError / 未捕获异常转 ApiResponse。"""

from __future__ import annotations

from fastapi import FastAPI, Request
from fastapi.exceptions import RequestValidationError
from fastapi.responses import JSONResponse
from loguru import logger

from ruisheng_shared.errors.codes import BizError, ErrCode

from .response import fail


def register_exception_handlers(app: FastAPI) -> None:
    @app.exception_handler(BizError)
    async def _biz(_: Request, exc: BizError) -> JSONResponse:
        return JSONResponse(
            status_code=exc.http_status,
            content=fail(exc.code, exc.msg).model_dump(),
        )

    @app.exception_handler(RequestValidationError)
    async def _validation(_: Request, exc: RequestValidationError) -> JSONResponse:
        return JSONResponse(
            status_code=400,
            content=fail(ErrCode.BAD_PARAM, str(exc.errors()[:3])).model_dump(),
        )

    @app.exception_handler(Exception)
    async def _generic(_: Request, exc: Exception) -> JSONResponse:
        logger.exception("unhandled exception: {!r}", exc)
        return JSONResponse(
            status_code=500,
            content=fail(ErrCode.INTERNAL, "internal server error").model_dump(),
        )
```

`ruisheng-api/src/ruisheng_api/core/__init__.py`:
```python
"""核心基础设施：错误、响应、安全、租户、RBAC。"""
```

- [ ] **Step 5：跑 → PASS**

- [ ] **Step 6：commit**

```bash
git add ruisheng-api/src/ruisheng_api/core ruisheng-api/tests/unit/core
git commit -m "feat(api): A4 ApiResponse + exception handlers

- core/response.py: ApiResponse(code/msg/data/transid), ok()/fail()
- core/errors.py: register_exception_handlers covers BizError,
  RequestValidationError (→ -100), generic Exception (→ -300 500)
- BizError.http_status drives HTTP status code via _HTTP_MAP"
```

### Task A5: main.py app factory + health stub + run entrypoint

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/main.py`
- Create: `ruisheng-api/src/ruisheng_api/api/__init__.py`
- Create: `ruisheng-api/src/ruisheng_api/api/health.py`
- Modify: `ruisheng-api/src/ruisheng_api/__main__.py`（改为启 uvicorn）
- Test: `ruisheng-api/tests/unit/test_main.py`

- [ ] **Step 1：写测试**

```python
# ruisheng-api/tests/unit/test_main.py
from fastapi.testclient import TestClient

from ruisheng_api.main import create_app


def test_app_health_liveness(monkeypatch):
    monkeypatch.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    monkeypatch.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    monkeypatch.setenv("API_REDIS_URL", "redis://:p@h/0")
    monkeypatch.setenv("API_JWT_SECRET", "x" * 64)

    app = create_app()
    r = TestClient(app).get("/api/health/live")
    assert r.status_code == 200
    assert r.json()["code"] == 0
    assert r.json()["data"]["status"] == "live"


def test_app_openapi_title():
    from ruisheng_api.config import Config
    import os

    for k, v in {
        "API_DB_URL": "postgresql+asyncpg://u:p@h/d",
        "API_GW_DB_URL": "postgresql+asyncpg://u:p@h/d",
        "API_REDIS_URL": "redis://:p@h/0",
        "API_JWT_SECRET": "x" * 64,
    }.items():
        os.environ[k] = v

    app = create_app()
    spec = app.openapi()
    assert spec["info"]["title"] == "ruisheng-api"
```

- [ ] **Step 2：跑 → FAIL**

- [ ] **Step 3：实现 api/health.py**

```python
"""健康检查：live（进程活）/ ready（DB+Redis 可达，Stage A5 只做 live 存根）。"""

from __future__ import annotations

from fastapi import APIRouter

from ..core.response import ok

router = APIRouter(prefix="/api/health", tags=["health"])


@router.get("/live")
async def liveness() -> dict[str, object]:
    return ok(data={"status": "live"}).model_dump()
```

`ruisheng-api/src/ruisheng_api/api/__init__.py`:
```python
"""REST 路由。mount_routers() 聚合挂载。"""

from fastapi import FastAPI

from . import health


def mount_routers(app: FastAPI) -> None:
    app.include_router(health.router)
```

- [ ] **Step 4：实现 main.py**

```python
"""FastAPI app factory + lifespan（Stage A5 只挂 health；后续 Stage 扩 lifespan 启 engine/redis/scheduler）。"""

from __future__ import annotations

from contextlib import asynccontextmanager
from typing import AsyncIterator

from fastapi import FastAPI

from .api import mount_routers
from .config import Config
from .core.errors import register_exception_handlers


@asynccontextmanager
async def _lifespan(app: FastAPI) -> AsyncIterator[None]:
    # Stage B/D 会在此装 engine / redis client
    yield


def create_app(config: Config | None = None) -> FastAPI:
    cfg = config or Config()
    app = FastAPI(
        title="ruisheng-api",
        version="0.1.0",
        lifespan=_lifespan,
        docs_url="/api/docs" if cfg.env != "prod" else None,
        openapi_url="/api/openapi.json" if cfg.env != "prod" else None,
    )
    app.state.config = cfg
    register_exception_handlers(app)
    mount_routers(app)
    return app
```

改 `ruisheng-api/src/ruisheng_api/__main__.py`:
```python
"""`python -m ruisheng_api` 启 uvicorn。"""

from __future__ import annotations

import uvicorn

from .config import Config


def main() -> None:
    cfg = Config()
    uvicorn.run(
        "ruisheng_api.main:create_app",
        factory=True,
        host=cfg.listen_host,
        port=cfg.listen_port,
        log_level="info",
    )


if __name__ == "__main__":
    main()
```

- [ ] **Step 5：跑 → PASS**

- [ ] **Step 6：commit**

```bash
git add ruisheng-api/src/ruisheng_api ruisheng-api/tests/unit/test_main.py
git commit -m "feat(api): A5 FastAPI app factory + /api/health/live

- main.create_app(): mounts routers, registers exception handlers,
  config loaded from env, lifespan reserved for engine/redis/scheduler
- api/health.py: GET /api/health/live → ApiResponse.ok(status=live)
- __main__: uvicorn.run(factory=True) entrypoint
- docs_url disabled in prod env"
```

### Task A6: Stage A tag + PROGRESS.md 更新

- [ ] **Step 1：在 master 更新 PROGRESS.md 添加 "Plan 2 Stage A 5/5 ✅" 块（列出 A1-A5 commits）**

- [ ] **Step 2：在 worktree 打 tag**

```bash
git tag plan-2-stage-a-complete
git push origin plan-2-stage-a-complete
```

- [ ] **Step 3：更新 memory/project_ruisheng_scada.md，记 Plan 2 Stage A 完成**

---

## Stage B: Security Core

### Task B1: logging_setup.py（loguru + 脱敏 filter）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/logging_setup.py`
- Test: `ruisheng-api/tests/unit/test_logging_setup.py`

**设计**：对应 spec §5.12.3 脱敏规则。loguru sink 通过 `patcher` 删改 record["extra"] 内敏感键；只记 INFO+；soft_logs 表写入留给 Stage K。

- [ ] **Step 1：写测试**

```python
# ruisheng-api/tests/unit/test_logging_setup.py
import io

from loguru import logger

from ruisheng_api.logging_setup import SENSITIVE_KEYS, configure_logging, redact_record


def test_redact_drops_password():
    rec = {"extra": {"password": "secret", "user": "a"}}
    redact_record(rec)
    assert rec["extra"]["password"] == "***"
    assert rec["extra"]["user"] == "a"


def test_redact_phone_masks_middle():
    rec = {"extra": {"phone_number": "13812345678"}}
    redact_record(rec)
    assert rec["extra"]["phone_number"] == "138****5678"


def test_redact_short_phone_fully_hidden():
    rec = {"extra": {"phone_number": "123"}}
    redact_record(rec)
    assert rec["extra"]["phone_number"] == "***"


def test_redact_keeps_other_keys():
    rec = {"extra": {"dev_number": "60270012", "random": 42}}
    redact_record(rec)
    assert rec["extra"]["random"] == 42


def test_configure_logging_idempotent():
    configure_logging(level="INFO")
    configure_logging(level="INFO")  # 二次调用不报错


def test_all_sensitive_keys_known():
    assert {"password", "secret", "token", "jwt", "authorization"}.issubset(SENSITIVE_KEYS)
```

- [ ] **Step 2：跑 → FAIL**

- [ ] **Step 3：实现 logging_setup.py**

```python
"""loguru 配置 + 敏感字段脱敏（对应 spec §5.12.3）。"""

from __future__ import annotations

import sys
from typing import Any

from loguru import logger

SENSITIVE_KEYS: frozenset[str] = frozenset(
    {
        "password",
        "password_hash",
        "secret",
        "appsecret",
        "api_key",
        "jwt",
        "token",
        "authorization",
        "cookie",
    }
)

_configured = False


def redact_record(record: dict[str, Any]) -> None:
    extra = record.get("extra") or {}
    for key in list(extra):
        low = key.lower()
        if low in SENSITIVE_KEYS:
            extra[key] = "***"
            continue
        if low in {"phone_number", "msisdn"}:
            v = str(extra[key])
            extra[key] = f"{v[:3]}****{v[-4:]}" if len(v) >= 7 else "***"


def _patcher(record: dict[str, Any]) -> None:
    redact_record(record)


def configure_logging(*, level: str = "INFO") -> None:
    global _configured
    logger.remove()
    logger.add(
        sys.stderr,
        level=level,
        serialize=True,  # 结构化 JSON
        enqueue=True,
        backtrace=False,
        diagnose=False,
    )
    logger.configure(patcher=_patcher)
    _configured = True
```

- [ ] **Step 4：跑 → PASS**

- [ ] **Step 5：commit**

```bash
git add ruisheng-api/src/ruisheng_api/logging_setup.py ruisheng-api/tests/unit/test_logging_setup.py
git commit -m "feat(api): B1 loguru + sensitive field redaction

- SENSITIVE_KEYS: password/secret/token/jwt/authorization/cookie/...
- phone_number: 138****5678 (len>=7) or *** (shorter)
- configure_logging(): serialize=True (JSON), enqueue=True (async)
- idempotent; safe to call twice on re-import"
```

### Task B2: security.py（JWT + bcrypt + ULID + fingerprint）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/core/security.py`
- Test: `ruisheng-api/tests/unit/core/test_security.py`

- [ ] **Step 1：写测试**

```python
# ruisheng-api/tests/unit/core/test_security.py
import pytest

from ruisheng_api.core.security import (
    client_fingerprint,
    hash_password,
    issue_access_token,
    issue_refresh_token,
    verify_password,
    verify_token,
)
from ruisheng_shared.errors.codes import BizError


def test_hash_and_verify():
    h = hash_password("hunter2")
    assert h != "hunter2"
    assert verify_password("hunter2", h) is True
    assert verify_password("wrong", h) is False


def test_issue_and_verify_access():
    fp = client_fingerprint("1.1.1.1", "ua-1")
    t = issue_access_token(
        "alice", "grp1", "User", 1, fp, secret="s" * 64, ttl_sec=900
    )
    payload = verify_token(t, secret="s" * 64, expected_fp=fp)
    assert payload["sub"] == "alice"
    assert payload["usr_group"] == "grp1"
    assert payload["role"] == "User"
    assert payload["ca"] == 1
    assert payload["fp"] == fp
    assert payload["typ"] == "access"


def test_verify_rejects_wrong_fingerprint():
    fp = client_fingerprint("1.1.1.1", "ua-1")
    t = issue_access_token("a", "g", "User", 0, fp, secret="s" * 64, ttl_sec=900)
    with pytest.raises(BizError):
        verify_token(t, secret="s" * 64, expected_fp="different")


def test_verify_rejects_expired():
    fp = client_fingerprint("x", "y")
    t = issue_access_token("a", "g", "User", 0, fp, secret="s" * 64, ttl_sec=-1)
    with pytest.raises(BizError):
        verify_token(t, secret="s" * 64, expected_fp=fp)


def test_refresh_token_has_typ_refresh():
    fp = client_fingerprint("x", "y")
    t = issue_refresh_token("a", "g", "User", 0, fp, secret="s" * 64, ttl_sec=3600)
    payload = verify_token(t, secret="s" * 64, expected_fp=fp, expected_typ="refresh")
    assert payload["typ"] == "refresh"


def test_fingerprint_stable():
    assert client_fingerprint("1.1.1.1", "ua") == client_fingerprint("1.1.1.1", "ua")
    assert client_fingerprint("1.1.1.1", "ua") != client_fingerprint("1.1.1.2", "ua")
```

- [ ] **Step 2：跑 → FAIL**

- [ ] **Step 3：实现 security.py**

```python
"""JWT 签发/校验 + bcrypt 密码 + client fingerprint。对应 spec §5.13。"""

from __future__ import annotations

import hashlib
import time

import ulid
from jose import JWTError, jwt
from passlib.context import CryptContext

from ruisheng_shared.errors.codes import BizError, ErrCode

_pwd = CryptContext(schemes=["bcrypt"], deprecated="auto")
_ALG = "HS256"


def hash_password(raw: str) -> str:
    return _pwd.hash(raw)


def verify_password(raw: str, hashed: str) -> bool:
    try:
        return _pwd.verify(raw, hashed)
    except ValueError:
        return False


def client_fingerprint(ip: str, user_agent: str) -> str:
    return hashlib.sha256(f"{ip}|{user_agent}".encode()).hexdigest()[:16]


def _issue(
    *,
    sub: str,
    usr_group: str,
    role: str,
    ca: int,
    fp: str,
    typ: str,
    secret: str,
    ttl_sec: int,
) -> str:
    now = int(time.time())
    payload = {
        "sub": sub,
        "usr_group": usr_group,
        "role": role,
        "ca": ca,
        "fp": fp,
        "jti": str(ulid.new()),
        "typ": typ,
        "iat": now,
        "exp": now + ttl_sec,
    }
    return jwt.encode(payload, secret, algorithm=_ALG)


def issue_access_token(
    sub: str, usr_group: str, role: str, ca: int, fp: str, *, secret: str, ttl_sec: int
) -> str:
    return _issue(
        sub=sub, usr_group=usr_group, role=role, ca=ca, fp=fp, typ="access",
        secret=secret, ttl_sec=ttl_sec,
    )


def issue_refresh_token(
    sub: str, usr_group: str, role: str, ca: int, fp: str, *, secret: str, ttl_sec: int
) -> str:
    return _issue(
        sub=sub, usr_group=usr_group, role=role, ca=ca, fp=fp, typ="refresh",
        secret=secret, ttl_sec=ttl_sec,
    )


def verify_token(
    token: str, *, secret: str, expected_fp: str, expected_typ: str = "access"
) -> dict[str, object]:
    try:
        payload = jwt.decode(token, secret, algorithms=[_ALG])
    except JWTError as e:
        raise BizError(ErrCode.UNAUTHED, f"invalid token: {e}") from e
    if payload.get("typ") != expected_typ:
        raise BizError(ErrCode.UNAUTHED, f"wrong token type: {payload.get('typ')}")
    if payload.get("fp") != expected_fp:
        raise BizError(ErrCode.UNAUTHED, "fingerprint mismatch")
    return payload
```

- [ ] **Step 4：跑 → PASS**

- [ ] **Step 5：commit**

```bash
git add ruisheng-api/src/ruisheng_api/core/security.py ruisheng-api/tests/unit/core/test_security.py
git commit -m "feat(api): B2 JWT issue/verify + bcrypt + client fingerprint

- hash_password/verify_password via passlib[bcrypt]
- issue_access_token / issue_refresh_token: typ=access/refresh, jti=ULID
- verify_token: rejects expired, wrong fingerprint, wrong typ → BizError UNAUTHED
- client_fingerprint: sha256(ip|ua)[:16] stable across calls"
```

### Task B3: core/tenant.py（SET LOCAL + ORM 过滤）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/core/tenant.py`
- Test: `ruisheng-api/tests/unit/core/test_tenant.py`

**设计**：对应 spec §3.7。api 角色每事务 `SET LOCAL app.tenant_id = :usr_group` + `SET LOCAL app.role = :role`。函数签名幂等；调用者（deps.py Stage B5）负责在 AsyncSession 事务内调用。

- [ ] **Step 1：写测试（mock session.execute）**

```python
# ruisheng-api/tests/unit/core/test_tenant.py
from unittest.mock import AsyncMock, MagicMock, call

import pytest

from ruisheng_api.core.tenant import (
    TENANT_CONTEXT_MISSING,
    TenantIsolationError,
    apply_tenant_context,
)


@pytest.mark.asyncio
async def test_apply_tenant_context_runs_set_local():
    session = MagicMock()
    session.execute = AsyncMock()
    await apply_tenant_context(session, usr_group="grp1", role="User")
    # 两次 SET LOCAL
    assert session.execute.await_count == 2


@pytest.mark.asyncio
async def test_apply_tenant_context_admin_may_have_empty_group():
    session = MagicMock()
    session.execute = AsyncMock()
    await apply_tenant_context(session, usr_group="", role="Administrators")
    # Administrators 允许空 usr_group；仍然 SET role
    assert session.execute.await_count == 2


@pytest.mark.asyncio
async def test_apply_tenant_context_non_admin_blank_raises():
    session = MagicMock()
    session.execute = AsyncMock()
    with pytest.raises(TenantIsolationError, match=TENANT_CONTEXT_MISSING):
        await apply_tenant_context(session, usr_group="", role="User")


@pytest.mark.asyncio
async def test_apply_tenant_context_invalid_role():
    session = MagicMock()
    session.execute = AsyncMock()
    with pytest.raises(TenantIsolationError):
        await apply_tenant_context(session, usr_group="g", role="Hacker")
```

- [ ] **Step 2：跑 → FAIL**

- [ ] **Step 3：实现 tenant.py**

```python
"""多租户：SET LOCAL app.tenant_id / app.role（对应 spec §3.7 1st layer）。"""

from __future__ import annotations

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

VALID_ROLES = frozenset({"Administrators", "GroupCompany", "Company", "User"})
TENANT_CONTEXT_MISSING = "query without tenant context"


class TenantIsolationError(RuntimeError):
    """租户上下文缺失或角色非法。"""


async def apply_tenant_context(session: AsyncSession, *, usr_group: str, role: str) -> None:
    if role not in VALID_ROLES:
        raise TenantIsolationError(f"invalid role: {role!r}")
    if role != "Administrators" and not usr_group:
        raise TenantIsolationError(TENANT_CONTEXT_MISSING)
    # PG 参数化 SET LOCAL 在 asyncpg 下须 text() + bind
    await session.execute(
        text("SELECT set_config('app.tenant_id', :v, true)"),
        {"v": usr_group or ""},
    )
    await session.execute(
        text("SELECT set_config('app.role', :v, true)"),
        {"v": role},
    )
```

- [ ] **Step 4：跑 → PASS**

- [ ] **Step 5：commit**

```bash
git add ruisheng-api/src/ruisheng_api/core/tenant.py ruisheng-api/tests/unit/core/test_tenant.py
git commit -m "feat(api): B3 apply_tenant_context SET LOCAL app.tenant_id/role

- VALID_ROLES = Administrators/GroupCompany/Company/User
- Administrators may pass empty usr_group (cross-tenant); others raise
- uses set_config(..., true) SQL (local-scope) via sqlalchemy text()
- TenantIsolationError for missing context / invalid role"
```

### Task B4: core/rbac.py + core/login_limit.py

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/core/rbac.py`
- Create: `ruisheng-api/src/ruisheng_api/core/login_limit.py`
- Test: `ruisheng-api/tests/unit/core/test_rbac.py`
- Test: `ruisheng-api/tests/unit/core/test_login_limit.py`

**设计**：
- `rbac.py`：`require_role(*roles)` / `require_ca(mask: int)` / `require_otp()`，返回 FastAPI Depends
- `login_limit.py`：Redis `INCR + EXPIRE` 分布式锁，按 spec §5.13

- [ ] **Step 1：写 rbac 测试**

```python
# ruisheng-api/tests/unit/core/test_rbac.py
import pytest

from ruisheng_api.core.rbac import CurrentUser, check_ca, check_role
from ruisheng_shared.errors.codes import BizError, ErrCode


def _user(role="User", ca=0):
    return CurrentUser(
        user_name="alice",
        usr_group="g1",
        role=role,
        control_authority=ca,
        jti="01HXY",
        fp="fp1",
    )


def test_check_role_pass():
    check_role(_user(role="Company"), allowed=("Company", "Administrators"))


def test_check_role_fail():
    with pytest.raises(BizError) as ei:
        check_role(_user(role="User"), allowed=("Company", "Administrators"))
    assert ei.value.code == ErrCode.FORBIDDEN


def test_check_ca_pass_bit0():
    check_ca(_user(ca=0b001), bit=0x01)


def test_check_ca_fail():
    with pytest.raises(BizError) as ei:
        check_ca(_user(ca=0b000), bit=0x01)
    assert ei.value.code == ErrCode.FORBIDDEN
```

- [ ] **Step 2：写 login_limit 测试（fakeredis）**

```python
# ruisheng-api/tests/unit/core/test_login_limit.py
import fakeredis.aioredis
import pytest

from ruisheng_api.core.login_limit import (
    clear_login_fail,
    is_user_locked,
    record_login_fail,
)


@pytest.mark.asyncio
async def test_record_increments_and_locks_after_threshold():
    r = fakeredis.aioredis.FakeRedis()
    for _ in range(4):
        assert (await record_login_fail(r, "alice", "1.1.1.1", user_max=5, ip_max=20,
                                        window=300, lock_ttl=1800, ip_block_ttl=3600)) is False
    # 第 5 次触发 lock
    assert (await record_login_fail(r, "alice", "1.1.1.1", user_max=5, ip_max=20,
                                    window=300, lock_ttl=1800, ip_block_ttl=3600)) is True
    assert await is_user_locked(r, "alice") is True


@pytest.mark.asyncio
async def test_clear_resets_counter():
    r = fakeredis.aioredis.FakeRedis()
    await record_login_fail(r, "a", "1.1.1.1", user_max=5, ip_max=20,
                            window=300, lock_ttl=1800, ip_block_ttl=3600)
    await clear_login_fail(r, "a")
    assert await is_user_locked(r, "a") is False


@pytest.mark.asyncio
async def test_ip_block_after_threshold():
    r = fakeredis.aioredis.FakeRedis()
    for _ in range(20):
        await record_login_fail(r, f"u{_}", "9.9.9.9", user_max=5, ip_max=20,
                                window=300, lock_ttl=1800, ip_block_ttl=3600)
    assert await r.exists("ip_block:9.9.9.9")
```

- [ ] **Step 3：跑 → FAIL**

- [ ] **Step 4：实现 rbac.py**

```python
"""4 级 RBAC + control_authority 位检查。对应 spec §3.6。"""

from __future__ import annotations

from dataclasses import dataclass

from ruisheng_shared.errors.codes import BizError, ErrCode


@dataclass(frozen=True)
class CurrentUser:
    user_name: str
    usr_group: str
    role: str
    control_authority: int
    jti: str
    fp: str


def check_role(user: CurrentUser, *, allowed: tuple[str, ...]) -> None:
    if user.role not in allowed:
        raise BizError(ErrCode.FORBIDDEN, f"role {user.role!r} not in {allowed}")


def check_ca(user: CurrentUser, *, bit: int) -> None:
    if not (user.control_authority & bit):
        raise BizError(ErrCode.FORBIDDEN, f"missing control_authority bit 0x{bit:x}")
```

- [ ] **Step 5：实现 login_limit.py**

```python
"""分布式登录失败计数 + 用户锁 + IP 黑名单（spec §5.13 B-S8）。"""

from __future__ import annotations

import redis.asyncio as redis_async


async def record_login_fail(
    r: redis_async.Redis,
    user_name: str,
    ip: str,
    *,
    user_max: int,
    ip_max: int,
    window: int,
    lock_ttl: int,
    ip_block_ttl: int,
) -> bool:
    """返回 True 若用户因本次失败被锁。"""
    key_user = f"login_fail:{user_name}"
    n_user = await r.incr(key_user)
    if n_user == 1:
        await r.expire(key_user, window)
    locked = False
    if n_user >= user_max:
        await r.setex(f"login_lock:{user_name}", lock_ttl, "1")
        locked = True

    key_ip = f"login_fail_ip:{ip}"
    n_ip = await r.incr(key_ip)
    if n_ip == 1:
        await r.expire(key_ip, window)
    if n_ip >= ip_max:
        await r.setex(f"ip_block:{ip}", ip_block_ttl, "1")
    return locked


async def is_user_locked(r: redis_async.Redis, user_name: str) -> bool:
    return bool(await r.exists(f"login_lock:{user_name}"))


async def is_ip_blocked(r: redis_async.Redis, ip: str) -> bool:
    return bool(await r.exists(f"ip_block:{ip}"))


async def clear_login_fail(r: redis_async.Redis, user_name: str) -> None:
    await r.delete(f"login_fail:{user_name}", f"login_lock:{user_name}")
```

- [ ] **Step 6：跑 → PASS**

- [ ] **Step 7：commit**

```bash
git add ruisheng-api/src/ruisheng_api/core/rbac.py ruisheng-api/src/ruisheng_api/core/login_limit.py ruisheng-api/tests/unit/core/test_rbac.py ruisheng-api/tests/unit/core/test_login_limit.py
git commit -m "feat(api): B4 RBAC + distributed login limiter

- rbac.CurrentUser dataclass (user_name/usr_group/role/ca/jti/fp)
- check_role / check_ca raise BizError FORBIDDEN (spec §3.6)
- login_limit: record_login_fail → returns True when user reaches user_max
  (lock via setex login_lock:{user}); IP ≥ ip_max → ip_block:{ip}
- is_user_locked / is_ip_blocked / clear_login_fail helpers"
```

### Task B5: deps.py（DI：db/redis/current_user/config）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/deps.py`
- Modify: `ruisheng-api/src/ruisheng_api/main.py`（lifespan 装 engine + redis + session_factory 到 app.state）
- Test: `ruisheng-api/tests/unit/test_deps.py`

**设计**：
- `get_config`（从 app.state）
- `get_session_factory`（从 app.state）
- `get_session`（开事务、`apply_tenant_context`、自动 commit/rollback）
- `get_redis`
- `get_current_user`（从 `Authorization: Bearer` 解 JWT，含 jti 黑名单检查）
- `get_optional_user`（可选登录）

- [ ] **Step 1：写测试（使用 TestClient + dependency_overrides）**

```python
# ruisheng-api/tests/unit/test_deps.py
import fakeredis.aioredis
import pytest
from fastapi.testclient import TestClient

from ruisheng_api.core.rbac import CurrentUser
from ruisheng_api.core.security import client_fingerprint, issue_access_token
from ruisheng_api.deps import get_current_user, get_redis
from ruisheng_api.main import create_app


def _base_env(m):
    m.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_REDIS_URL", "redis://:p@h/0")
    m.setenv("API_JWT_SECRET", "x" * 64)


def test_get_current_user_missing_header(monkeypatch):
    _base_env(monkeypatch)
    app = create_app()
    fake = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: fake

    @app.get("/me")
    async def me(u: CurrentUser = __import__("fastapi").Depends(get_current_user)):
        return {"u": u.user_name}

    r = TestClient(app).get("/me")
    assert r.status_code == 401
    assert r.json()["code"] == -101


def test_get_current_user_valid(monkeypatch):
    _base_env(monkeypatch)
    app = create_app()
    fake = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: fake

    from fastapi import Depends

    @app.get("/me")
    async def me(u: CurrentUser = Depends(get_current_user)):
        return {"u": u.user_name, "role": u.role}

    client = TestClient(app)
    fp = client_fingerprint("testclient", "testclient")
    tok = issue_access_token(
        "alice", "g1", "User", 1, fp, secret="x" * 64, ttl_sec=900
    )
    r = client.get("/me", headers={"Authorization": f"Bearer {tok}"})
    assert r.status_code == 200
    assert r.json() == {"u": "alice", "role": "User"}


def test_jti_blacklisted_returns_401(monkeypatch):
    _base_env(monkeypatch)
    app = create_app()
    fake = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: fake

    from fastapi import Depends

    @app.get("/me")
    async def me(u: CurrentUser = Depends(get_current_user)):
        return {}

    client = TestClient(app)
    fp = client_fingerprint("testclient", "testclient")
    tok = issue_access_token("a", "g", "User", 0, fp, secret="x" * 64, ttl_sec=900)
    from jose import jwt

    jti = jwt.decode(tok, "x" * 64, algorithms=["HS256"])["jti"]

    import asyncio

    asyncio.get_event_loop().run_until_complete(fake.sadd("jwt_blacklist", jti))

    r = client.get("/me", headers={"Authorization": f"Bearer {tok}"})
    assert r.status_code == 401
```

- [ ] **Step 2：跑 → FAIL**

- [ ] **Step 3：实现 deps.py**

```python
"""依赖注入：config / db session / redis / current_user。"""

from __future__ import annotations

from contextlib import asynccontextmanager
from typing import AsyncIterator

import redis.asyncio as redis_async
from fastapi import Depends, Header, Request
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker

from .config import Config
from .core.rbac import CurrentUser
from .core.security import client_fingerprint, verify_token
from .core.tenant import apply_tenant_context
from ruisheng_shared.errors.codes import BizError, ErrCode


def get_config(request: Request) -> Config:
    return request.app.state.config  # type: ignore[no-any-return]


def get_session_factory(request: Request) -> async_sessionmaker[AsyncSession]:
    return request.app.state.session_factory  # type: ignore[no-any-return]


def get_redis(request: Request) -> redis_async.Redis:
    return request.app.state.redis  # type: ignore[no-any-return]


async def get_session(
    factory: async_sessionmaker[AsyncSession] = Depends(get_session_factory),
) -> AsyncIterator[AsyncSession]:
    async with factory() as session:
        yield session


async def _extract_bearer(authorization: str | None) -> str:
    if not authorization or not authorization.lower().startswith("bearer "):
        raise BizError(ErrCode.UNAUTHED, "missing Authorization header")
    return authorization.split(" ", 1)[1].strip()


async def get_current_user(
    request: Request,
    authorization: str | None = Header(default=None),
    cfg: Config = Depends(get_config),
    r: redis_async.Redis = Depends(get_redis),
) -> CurrentUser:
    token = await _extract_bearer(authorization)
    client_host = request.client.host if request.client else "unknown"
    ua = request.headers.get("user-agent", "")
    fp = client_fingerprint(client_host, ua)
    payload = verify_token(token, secret=cfg.jwt_secret, expected_fp=fp)
    jti = str(payload.get("jti") or "")
    if await r.sismember("jwt_blacklist", jti):
        raise BizError(ErrCode.UNAUTHED, "token revoked")
    return CurrentUser(
        user_name=str(payload["sub"]),
        usr_group=str(payload.get("usr_group") or ""),
        role=str(payload["role"]),
        control_authority=int(payload.get("ca") or 0),
        jti=jti,
        fp=fp,
    )


@asynccontextmanager
async def db_tx(
    session: AsyncSession, user: CurrentUser
) -> AsyncIterator[AsyncSession]:
    """封装事务 + SET LOCAL tenant context。控制器用 `async with db_tx(s, u):`。"""
    async with session.begin():
        await apply_tenant_context(
            session, usr_group=user.usr_group, role=user.role
        )
        yield session
```

- [ ] **Step 4：更新 main.py lifespan 装 engine + redis**

```python
# ruisheng-api/src/ruisheng_api/main.py
from __future__ import annotations

from contextlib import asynccontextmanager
from typing import AsyncIterator

import redis.asyncio as redis_async
from fastapi import FastAPI

from .api import mount_routers
from .config import Config
from .core.errors import register_exception_handlers
from .db.base import build_engine, build_session_factory
from .logging_setup import configure_logging


@asynccontextmanager
async def _lifespan(app: FastAPI) -> AsyncIterator[None]:
    cfg: Config = app.state.config
    configure_logging(level="INFO")
    engine = build_engine(
        cfg.db_url, pool_size=cfg.db_pool_size, max_overflow=cfg.db_pool_overflow
    )
    app.state.engine = engine
    app.state.session_factory = build_session_factory(engine)
    app.state.redis = redis_async.from_url(cfg.redis_url, decode_responses=True)
    try:
        yield
    finally:
        await app.state.redis.aclose()
        await engine.dispose()


def create_app(config: Config | None = None) -> FastAPI:
    cfg = config or Config()
    app = FastAPI(
        title="ruisheng-api",
        version="0.1.0",
        lifespan=_lifespan,
        docs_url="/api/docs" if cfg.env != "prod" else None,
        openapi_url="/api/openapi.json" if cfg.env != "prod" else None,
    )
    app.state.config = cfg
    register_exception_handlers(app)
    mount_routers(app)
    return app
```

- [ ] **Step 5：跑 → PASS**

- [ ] **Step 6：commit**

```bash
git add ruisheng-api/src/ruisheng_api/deps.py ruisheng-api/src/ruisheng_api/main.py ruisheng-api/tests/unit/test_deps.py
git commit -m "feat(api): B5 dependency injection + lifespan engine/redis wiring

- deps.get_config/get_session_factory/get_redis from app.state
- deps.get_current_user: Authorization Bearer, verify_token,
  jwt_blacklist set check, build CurrentUser
- deps.db_tx(): async with session.begin() + apply_tenant_context
- main.lifespan: build_engine + session_factory + redis.from_url;
  dispose on shutdown
- logging configured at startup"
```

### Task B6: Stage B tag + PROGRESS 更新

同 Task A6 模板。tag `plan-2-stage-b-complete`。

---

## Stage C: Auth API

### Task C1: POST /api/auth/login（bcrypt + fingerprint + login_limit）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/auth.py`
- Create: `ruisheng-api/src/ruisheng_api/api/auth.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/users.py`
- Modify: `ruisheng-api/src/ruisheng_api/api/__init__.py`（mount auth router）
- Test: `ruisheng-api/tests/unit/api/test_auth_login.py`

- [ ] **Step 1：写 schema**

```python
# ruisheng-api/src/ruisheng_api/api/schemas/auth.py
from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field


class LoginRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    user_name: str = Field(..., min_length=3, max_length=50)
    password: str = Field(..., min_length=6, max_length=200)


class LoginResponseData(BaseModel):
    access_token: str
    refresh_token: str
    access_ttl_sec: int
    user_name: str
    role: str
    usr_group: str
    control_authority: int


class RefreshRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    refresh_token: str


class LogoutRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    refresh_token: str | None = None


class OtpSendRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    action: str = Field(..., pattern="^[a-z_]{3,32}$")
    channel: str = Field(default="sms", pattern="^(sms|email|wechat)$")


class SmsSendRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    action: str = Field(..., pattern="^[a-z_]{3,32}$")
    channel: str = Field(default="sms", pattern="^(sms|email|wechat)$")
    phone_number: str = Field(..., pattern=r"^1[3-9][0-9]{9}$")


class RegisterRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    user_name: str = Field(..., pattern=r"^1[3-9][0-9]{9}$")
    password: str = Field(..., min_length=8, max_length=200)
    otp_code: str = Field(..., pattern=r"^[0-9]{6}$")
```

- [ ] **Step 2：写单测（mock repository + fake redis）**

```python
# ruisheng-api/tests/unit/api/test_auth_login.py
import fakeredis.aioredis
from fastapi.testclient import TestClient

from ruisheng_api.core.security import hash_password
from ruisheng_api.deps import get_redis, get_session
from ruisheng_api.main import create_app
from ruisheng_api.db.repositories import users as users_repo


def _env(m):
    m.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_REDIS_URL", "redis://:p@h/0")
    m.setenv("API_JWT_SECRET", "x" * 64)


class _FakeUser:
    def __init__(self, *, user_name, password_hash, authority, usr_group, ca=0):
        self.user_name = user_name
        self.password_hash = password_hash
        self.authority = authority
        self.usr_group = usr_group
        self.control_authority = ca
        self.deleted_at = None


def _install(app, monkeypatch, user):
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r

    async def fake_session():
        yield object()

    app.dependency_overrides[get_session] = fake_session

    async def fake_load(session, uname):
        return user if user and uname == user.user_name else None

    monkeypatch.setattr(users_repo, "load_by_user_name", fake_load)
    return r


def test_login_happy(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    _install(app, monkeypatch, _FakeUser(
        user_name="alice", password_hash=hash_password("hunter2"),
        authority="User", usr_group="g1",
    ))
    resp = TestClient(app).post(
        "/api/auth/login", json={"user_name": "alice", "password": "hunter2"}
    )
    assert resp.status_code == 200
    data = resp.json()["data"]
    assert data["access_token"] and data["refresh_token"]
    assert data["role"] == "User"


def test_login_wrong_password(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    _install(app, monkeypatch, _FakeUser(
        user_name="alice", password_hash=hash_password("hunter2"),
        authority="User", usr_group="g1",
    ))
    resp = TestClient(app).post(
        "/api/auth/login", json={"user_name": "alice", "password": "nope"}
    )
    assert resp.status_code == 401


def test_login_unknown_user(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    _install(app, monkeypatch, None)
    resp = TestClient(app).post(
        "/api/auth/login", json={"user_name": "nobody", "password": "x"}
    )
    assert resp.status_code == 401


def test_login_locked_user(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = _install(app, monkeypatch, _FakeUser(
        user_name="alice", password_hash=hash_password("hunter2"),
        authority="User", usr_group="g1",
    ))
    import asyncio
    asyncio.get_event_loop().run_until_complete(
        r.setex("login_lock:alice", 1800, "1")
    )
    resp = TestClient(app).post(
        "/api/auth/login", json={"user_name": "alice", "password": "hunter2"}
    )
    assert resp.status_code == 403
```

- [ ] **Step 3：跑 → FAIL**

- [ ] **Step 4：实现 repositories/users.py（最小版）**

```python
"""users 仓储（Stage C1：load_by_user_name / create_user）。"""

from __future__ import annotations

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from ruisheng_shared.models.users import User


async def load_by_user_name(session: AsyncSession, user_name: str) -> User | None:
    stmt = select(User).where(User.user_name == user_name, User.deleted_at.is_(None))
    result = await session.execute(stmt)
    return result.scalar_one_or_none()


async def create_user(
    session: AsyncSession,
    *,
    user_name: str,
    password_hash: str,
    authority: str,
    usr_group: str,
    control_authority: int = 0,
) -> User:
    u = User(
        user_name=user_name,
        password_hash=password_hash,
        authority=authority,
        usr_group=usr_group,
        control_authority=control_authority,
    )
    session.add(u)
    await session.flush()
    return u
```

`ruisheng-api/src/ruisheng_api/db/repositories/__init__.py` 空文件即可。

- [ ] **Step 5：实现 api/auth.py**

```python
"""Auth API：/api/auth/*。"""

from __future__ import annotations

import redis.asyncio as redis_async
from fastapi import APIRouter, Depends, Request
from loguru import logger
from sqlalchemy.ext.asyncio import AsyncSession

from ..config import Config
from ..core.login_limit import (
    clear_login_fail, is_ip_blocked, is_user_locked, record_login_fail,
)
from ..core.response import ApiResponse, ok
from ..core.security import (
    client_fingerprint, hash_password, issue_access_token,
    issue_refresh_token, verify_password,
)
from ..db.repositories import users as users_repo
from ..deps import get_config, get_redis, get_session
from .schemas.auth import LoginRequest, LoginResponseData
from ruisheng_shared.errors.codes import BizError, ErrCode

router = APIRouter(prefix="/api/auth", tags=["auth"])


@router.post("/login", response_model=ApiResponse)
async def login(
    body: LoginRequest,
    request: Request,
    cfg: Config = Depends(get_config),
    r: redis_async.Redis = Depends(get_redis),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    ip = request.client.host if request.client else "unknown"
    ua = request.headers.get("user-agent", "")
    if await is_user_locked(r, body.user_name):
        raise BizError(ErrCode.FORBIDDEN, "account locked; try later")
    if await is_ip_blocked(r, ip):
        raise BizError(ErrCode.FORBIDDEN, "ip temporarily blocked")
    user = await users_repo.load_by_user_name(session, body.user_name)
    if user is None or not verify_password(body.password, user.password_hash):
        await record_login_fail(
            r, body.user_name, ip,
            user_max=cfg.login_fail_user_max,
            ip_max=cfg.login_fail_ip_max,
            window=cfg.login_fail_user_window_sec,
            lock_ttl=cfg.login_lock_ttl_sec,
            ip_block_ttl=cfg.ip_block_ttl_sec,
        )
        raise BizError(ErrCode.UNAUTHED, "invalid credentials")
    await clear_login_fail(r, body.user_name)
    fp = client_fingerprint(ip, ua)
    access = issue_access_token(
        user.user_name, user.usr_group, user.authority, user.control_authority,
        fp, secret=cfg.jwt_secret, ttl_sec=cfg.jwt_access_ttl_sec,
    )
    refresh = issue_refresh_token(
        user.user_name, user.usr_group, user.authority, user.control_authority,
        fp, secret=cfg.jwt_secret, ttl_sec=cfg.jwt_refresh_ttl_sec,
    )
    logger.bind(user_name=user.user_name, usr_group=user.usr_group).info("login ok")
    return ok(data=LoginResponseData(
        access_token=access, refresh_token=refresh,
        access_ttl_sec=cfg.jwt_access_ttl_sec,
        user_name=user.user_name, role=user.authority,
        usr_group=user.usr_group, control_authority=user.control_authority,
    ).model_dump())
```

更新 `api/__init__.py`：mount auth router（include `auth.router`）。

- [ ] **Step 6：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): C1 POST /api/auth/login bcrypt + fingerprint + login limiter

- LoginRequest/LoginResponseData/RegisterRequest/OtpSendRequest/... schemas
- bcrypt verify_password; on mismatch record_login_fail (user+ip)
- pre-check is_user_locked / is_ip_blocked → 403
- issue access(15m) + refresh(7d) tokens with fingerprint binding
- users_repo.load_by_user_name + create_user via ruisheng_shared.models.User"
```

### Task C2: POST /api/auth/sms/send + POST /api/auth/register（OTP 流程）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/services/otp.py`
- Modify: `ruisheng-api/src/ruisheng_api/api/auth.py`（+/sms/send, /register）
- Modify: `ruisheng-api/src/ruisheng_api/config.py`（+ default_usr_group）
- Test: `ruisheng-api/tests/unit/services/test_otp.py`
- Test: `ruisheng-api/tests/unit/api/test_auth_register.py`

- [ ] **Step 1：写 OTP 单测**

```python
# ruisheng-api/tests/unit/services/test_otp.py
import fakeredis.aioredis
import pytest

from ruisheng_api.services.otp import generate_otp, issue_otp, verify_otp


def test_generate_otp_6_digits():
    for _ in range(20):
        c = generate_otp()
        assert len(c) == 6 and c.isdigit()


@pytest.mark.asyncio
async def test_issue_and_verify_otp():
    r = fakeredis.aioredis.FakeRedis()
    code = await issue_otp(r, action="register", key="138", ttl_sec=300)
    assert await verify_otp(r, action="register", key="138", code=code) is True


@pytest.mark.asyncio
async def test_verify_consumes():
    r = fakeredis.aioredis.FakeRedis()
    code = await issue_otp(r, action="register", key="138", ttl_sec=300)
    assert await verify_otp(r, action="register", key="138", code=code) is True
    assert await verify_otp(r, action="register", key="138", code=code) is False


@pytest.mark.asyncio
async def test_verify_wrong_code():
    r = fakeredis.aioredis.FakeRedis()
    await issue_otp(r, action="register", key="138", ttl_sec=300)
    assert await verify_otp(r, action="register", key="138", code="000000") is False
```

- [ ] **Step 2：实现 services/otp.py**

```python
"""OTP 发码/校验，Redis GETDEL 消费（spec §5.13）。"""

from __future__ import annotations

import secrets

import redis.asyncio as redis_async


def generate_otp() -> str:
    return f"{secrets.randbelow(1_000_000):06d}"


def _key(action: str, key: str) -> str:
    return f"otp:{action}:{key}"


async def issue_otp(
    r: redis_async.Redis, *, action: str, key: str, ttl_sec: int
) -> str:
    code = generate_otp()
    await r.setex(_key(action, key), ttl_sec, code)
    return code


async def verify_otp(
    r: redis_async.Redis, *, action: str, key: str, code: str
) -> bool:
    stored = await r.getdel(_key(action, key))
    return stored is not None and str(stored) == code
```

`ruisheng-api/src/ruisheng_api/services/__init__.py` 空。

- [ ] **Step 3：写注册单测**

```python
# ruisheng-api/tests/unit/api/test_auth_register.py
import fakeredis.aioredis
from fastapi.testclient import TestClient

from ruisheng_api.deps import get_redis, get_session
from ruisheng_api.main import create_app
from ruisheng_api.services import otp as otp_svc
from ruisheng_api.db.repositories import users as users_repo


def _env(m):
    m.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_REDIS_URL", "redis://:p@h/0")
    m.setenv("API_JWT_SECRET", "x" * 64)


def test_sms_send(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r
    resp = TestClient(app).post(
        "/api/auth/sms/send",
        json={"action": "register", "channel": "sms", "phone_number": "13812345678"},
    )
    assert resp.status_code == 200
    assert resp.json()["code"] == 0


def test_register_happy(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r

    async def fake_session():
        yield object()

    app.dependency_overrides[get_session] = fake_session

    created = {}
    async def fake_create(session, **fields):
        created.update(fields)
        return type("U", (), {**fields, "id": 1})()
    async def fake_load(session, uname):
        return None

    monkeypatch.setattr(users_repo, "create_user", fake_create)
    monkeypatch.setattr(users_repo, "load_by_user_name", fake_load)

    import asyncio
    code = asyncio.get_event_loop().run_until_complete(
        otp_svc.issue_otp(r, action="register", key="13812345678", ttl_sec=300)
    )
    resp = TestClient(app).post(
        "/api/auth/register",
        json={"user_name": "13812345678", "password": "hunter2xx", "otp_code": code},
    )
    assert resp.status_code == 200
    assert created["user_name"] == "13812345678"
    assert created["authority"] == "User"


def test_register_bad_otp(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r

    async def fake_session():
        yield object()

    app.dependency_overrides[get_session] = fake_session
    resp = TestClient(app).post(
        "/api/auth/register",
        json={"user_name": "13812345678", "password": "hunter2xx", "otp_code": "000000"},
    )
    assert resp.status_code == 401
```

- [ ] **Step 4：扩 config.py + auth.py**

config.py 加字段：
```python
    default_usr_group: str = Field(default="default", min_length=1)
```

auth.py 扩：
```python
from .schemas.auth import RegisterRequest, SmsSendRequest
from ..services import otp as otp_svc


async def _send_sms(channel: str, phone: str, code: str) -> None:
    # Stage J 接入真适配器
    logger.bind(phone_number=phone, channel=channel).info("otp issued")


@router.post("/sms/send", response_model=ApiResponse)
async def sms_send(
    body: SmsSendRequest,
    cfg: Config = Depends(get_config),
    r: redis_async.Redis = Depends(get_redis),
) -> ApiResponse:
    code = await otp_svc.issue_otp(
        r, action=body.action, key=body.phone_number, ttl_sec=cfg.otp_ttl_sec
    )
    await _send_sms(body.channel, body.phone_number, code)
    return ok(data={"sent": True, "ttl_sec": cfg.otp_ttl_sec})


@router.post("/register", response_model=ApiResponse)
async def register(
    body: RegisterRequest,
    cfg: Config = Depends(get_config),
    r: redis_async.Redis = Depends(get_redis),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    if not await otp_svc.verify_otp(
        r, action="register", key=body.user_name, code=body.otp_code
    ):
        raise BizError(ErrCode.UNAUTHED, "otp invalid or expired")
    if await users_repo.load_by_user_name(session, body.user_name):
        raise BizError(ErrCode.BAD_PARAM, "user_name already registered")
    await users_repo.create_user(
        session,
        user_name=body.user_name,
        password_hash=hash_password(body.password),
        authority="User",
        usr_group=cfg.default_usr_group,
    )
    return ok(data={"user_name": body.user_name})
```

- [ ] **Step 5：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): C2 register + sms/send (OTP via Redis GETDEL)

- services/otp.py: issue_otp/verify_otp/generate_otp (6 digits)
- POST /api/auth/sms/send: pattern phone/action/channel; issue OTP (ttl=5min)
- POST /api/auth/register: verify OTP (GETDEL consume) → create_user
  with authority=User + config.default_usr_group
- Config.default_usr_group default='default' (Stage L wires wx_groups lookup)"
```

### Task C3: POST /api/auth/refresh + POST /api/auth/logout + /otp/send

**Files:**
- Modify: `ruisheng-api/src/ruisheng_api/api/auth.py`
- Test: `ruisheng-api/tests/unit/api/test_auth_refresh_logout.py`

**设计**：
- refresh：验 refresh_token → 旧 jti 进 jwt_blacklist → 发新 access + 新 refresh
- logout：吊销 access jti；若带 refresh 也吊销
- /otp/send：登录用户获 action OTP（L4 高危二次验证）

- [ ] **Step 1：写测试**

```python
# ruisheng-api/tests/unit/api/test_auth_refresh_logout.py
import fakeredis.aioredis
from fastapi.testclient import TestClient
from jose import jwt

from ruisheng_api.core.security import (
    client_fingerprint, issue_access_token, issue_refresh_token,
)
from ruisheng_api.deps import get_redis
from ruisheng_api.main import create_app


def _env(m):
    m.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_REDIS_URL", "redis://:p@h/0")
    m.setenv("API_JWT_SECRET", "x" * 64)


def test_refresh_rotates(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r
    fp = client_fingerprint("testclient", "testclient")
    old = issue_refresh_token("a", "g", "User", 0, fp, secret="x"*64, ttl_sec=3600)
    resp = TestClient(app).post("/api/auth/refresh", json={"refresh_token": old})
    assert resp.status_code == 200
    data = resp.json()["data"]
    assert data["access_token"] and data["refresh_token"] != old
    import asyncio
    old_jti = jwt.decode(old, "x"*64, algorithms=["HS256"])["jti"]
    assert asyncio.get_event_loop().run_until_complete(
        r.sismember("jwt_blacklist", old_jti)
    )


def test_refresh_rejects_access_token(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r
    fp = client_fingerprint("testclient", "testclient")
    access = issue_access_token("a", "g", "User", 0, fp, secret="x"*64, ttl_sec=900)
    assert TestClient(app).post(
        "/api/auth/refresh", json={"refresh_token": access}
    ).status_code == 401


def test_logout_blacklists_jti(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r
    fp = client_fingerprint("testclient", "testclient")
    access = issue_access_token("a", "g", "User", 0, fp, secret="x"*64, ttl_sec=900)
    jti = jwt.decode(access, "x"*64, algorithms=["HS256"])["jti"]
    resp = TestClient(app).post(
        "/api/auth/logout", json={}, headers={"Authorization": f"Bearer {access}"}
    )
    assert resp.status_code == 200
    import asyncio
    assert asyncio.get_event_loop().run_until_complete(
        r.sismember("jwt_blacklist", jti)
    )


def test_otp_send_requires_login(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r
    assert TestClient(app).post(
        "/api/auth/otp/send", json={"action": "cross_tenant", "channel": "sms"}
    ).status_code == 401
```

- [ ] **Step 2：实现（补 auth.py）**

```python
import time
from .schemas.auth import LogoutRequest, OtpSendRequest, RefreshRequest
from ..core.rbac import CurrentUser
from ..core.security import verify_token
from ..deps import get_current_user


async def _blacklist_jti(r: redis_async.Redis, jti: str, remaining: int) -> None:
    if remaining <= 0:
        return
    await r.sadd("jwt_blacklist", jti)
    await r.setex(f"jwt_blacklist_ttl:{jti}", remaining, "1")


@router.post("/refresh", response_model=ApiResponse)
async def refresh(
    body: RefreshRequest,
    request: Request,
    cfg: Config = Depends(get_config),
    r: redis_async.Redis = Depends(get_redis),
) -> ApiResponse:
    ip = request.client.host if request.client else "unknown"
    ua = request.headers.get("user-agent", "")
    fp = client_fingerprint(ip, ua)
    payload = verify_token(
        body.refresh_token, secret=cfg.jwt_secret,
        expected_fp=fp, expected_typ="refresh",
    )
    old_jti = str(payload["jti"])
    if await r.sismember("jwt_blacklist", old_jti):
        raise BizError(ErrCode.UNAUTHED, "refresh revoked")
    remaining = int(payload["exp"]) - int(time.time())
    await _blacklist_jti(r, old_jti, remaining)
    sub, grp, role = str(payload["sub"]), str(payload["usr_group"]), str(payload["role"])
    ca = int(payload.get("ca") or 0)
    access = issue_access_token(
        sub, grp, role, ca, fp, secret=cfg.jwt_secret, ttl_sec=cfg.jwt_access_ttl_sec
    )
    new_refresh = issue_refresh_token(
        sub, grp, role, ca, fp, secret=cfg.jwt_secret, ttl_sec=cfg.jwt_refresh_ttl_sec
    )
    return ok(data={
        "access_token": access, "refresh_token": new_refresh,
        "access_ttl_sec": cfg.jwt_access_ttl_sec,
    })


@router.post("/logout", response_model=ApiResponse)
async def logout(
    body: LogoutRequest,
    request: Request,
    cfg: Config = Depends(get_config),
    r: redis_async.Redis = Depends(get_redis),
    user: CurrentUser = Depends(get_current_user),
) -> ApiResponse:
    await _blacklist_jti(r, user.jti, cfg.jwt_access_ttl_sec)
    if body.refresh_token:
        ip = request.client.host if request.client else "unknown"
        ua = request.headers.get("user-agent", "")
        fp = client_fingerprint(ip, ua)
        try:
            p = verify_token(
                body.refresh_token, secret=cfg.jwt_secret,
                expected_fp=fp, expected_typ="refresh",
            )
            await _blacklist_jti(r, str(p["jti"]), int(p["exp"]) - int(time.time()))
        except BizError:
            pass
    return ok(data={"logout": True})


@router.post("/otp/send", response_model=ApiResponse)
async def otp_send(
    body: OtpSendRequest,
    cfg: Config = Depends(get_config),
    r: redis_async.Redis = Depends(get_redis),
    user: CurrentUser = Depends(get_current_user),
) -> ApiResponse:
    code = await otp_svc.issue_otp(
        r, action=body.action, key=user.user_name, ttl_sec=cfg.otp_ttl_sec
    )
    await _send_sms(body.channel, user.user_name, code)
    logger.bind(user_name=user.user_name, action=body.action).info("otp to user")
    return ok(data={"sent": True, "ttl_sec": cfg.otp_ttl_sec})
```

- [ ] **Step 3：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): C3 refresh/logout/otp-send with jti blacklist rotation

- /refresh: verify(typ=refresh) + fp → SADD old jti → issue new pair
- /logout: blacklist access jti + optional refresh jti (best-effort)
- /otp/send: logged-in user gets action OTP (L4 高危二次验证 §3.6)
- access tokens can't be used as refresh (typ mismatch → 401)"
```

### Task C4: Stage C tag + PROGRESS

- [ ] **Step 1：master 更新 PROGRESS 追加 Stage C 3/3 ✅**

- [ ] **Step 2：tag**

```bash
git tag plan-2-stage-c-complete
git push origin plan-2-stage-c-complete
```

---

## Stage D: Devices API

### Task D1: GET /api/devices + GET /api/devices/{dev_number}（带租户隔离）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/devices.py`
- Create: `ruisheng-api/src/ruisheng_api/api/devices.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/devices.py`
- Modify: `ruisheng-api/src/ruisheng_api/api/__init__.py`（mount devices）
- Create: `ruisheng-api/tests/integration/api/test_devices_crud.py`（需 PG）
- Test: `ruisheng-api/tests/unit/api/test_devices_list.py`（mock repo）

- [ ] **Step 1：schema**

```python
# ruisheng-api/src/ruisheng_api/api/schemas/devices.py
from __future__ import annotations
from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field


class DeviceOut(BaseModel):
    model_config = ConfigDict(from_attributes=True, extra="ignore")
    id: int
    dev_number: str
    dev_ser_number: str
    dev_name: str | None
    dev_type: str | None
    modbus_addr: int
    baud_rate: int | None
    is_online: bool
    last_call_at: datetime | None
    last_back_at: datetime | None
    loss_count: int
    update_interval_decisec: int
    group_company: str | None
    company: str | None
    department: str | None
    usr_group: str


class DeviceCreateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    dev_number: str = Field(..., pattern=r"^[A-Za-z0-9_\-]{3,50}$")
    dev_ser_number: str = Field(..., min_length=3, max_length=50)
    iccid: str | None = Field(default=None, max_length=50)
    dev_name: str | None = Field(default=None, max_length=100)
    dev_type: str | None = Field(default=None, max_length=50)
    modbus_addr: int = Field(..., ge=1, le=247)
    baud_rate: int | None = Field(default=None)
    update_interval_decisec: int = Field(default=100, ge=10, le=1000)
    group_company: str | None = None
    company: str | None = None
    department: str | None = None


class DeviceUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    dev_name: str | None = None
    dev_type: str | None = None
    baud_rate: int | None = None
    update_interval_decisec: int | None = Field(default=None, ge=10, le=1000)
    group_company: str | None = None
    company: str | None = None
    department: str | None = None


class DeviceListQuery(BaseModel):
    model_config = ConfigDict(extra="forbid")
    offset: int = Field(default=0, ge=0)
    limit: int = Field(default=50, ge=1, le=500)
    online_only: bool = False
    q: str | None = Field(default=None, max_length=100)
```

- [ ] **Step 2：repositories/devices.py**

```python
"""devices 仓储。"""

from __future__ import annotations

from sqlalchemy import func, or_, select
from sqlalchemy.ext.asyncio import AsyncSession

from ruisheng_shared.models.devices import Device


async def list_devices(
    session: AsyncSession, *, offset: int, limit: int,
    online_only: bool = False, q: str | None = None,
) -> tuple[list[Device], int]:
    base = select(Device).where(Device.deleted_at.is_(None))
    if online_only:
        base = base.where(Device.is_online.is_(True))
    if q:
        like = f"%{q}%"
        base = base.where(or_(Device.dev_number.ilike(like), Device.dev_name.ilike(like)))
    total = (await session.execute(
        select(func.count()).select_from(base.subquery())
    )).scalar_one()
    stmt = base.order_by(Device.id.desc()).offset(offset).limit(limit)
    rows = list((await session.execute(stmt)).scalars())
    return rows, int(total)


async def get_by_dev_number(session: AsyncSession, dev_number: str) -> Device | None:
    stmt = select(Device).where(
        Device.dev_number == dev_number, Device.deleted_at.is_(None)
    )
    return (await session.execute(stmt)).scalar_one_or_none()


async def create_device(session: AsyncSession, **fields) -> Device:
    d = Device(**fields)
    session.add(d)
    await session.flush()
    return d


async def update_device_fields(session: AsyncSession, device: Device, updates: dict) -> Device:
    for k, v in updates.items():
        setattr(device, k, v)
    await session.flush()
    return device


async def soft_delete(session: AsyncSession, device: Device) -> None:
    from datetime import datetime, timezone
    device.deleted_at = datetime.now(timezone.utc)
    await session.flush()
```

- [ ] **Step 3：api/devices.py**

```python
"""Devices API：/api/devices/*。"""

from __future__ import annotations

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from ..core.rbac import CurrentUser, check_ca, check_role
from ..core.response import ApiResponse, ok
from ..core.tenant import apply_tenant_context
from ..db.repositories import devices as devices_repo
from ..deps import get_current_user, get_session
from .schemas.devices import (
    DeviceCreateRequest, DeviceOut, DeviceUpdateRequest,
)
from ruisheng_shared.errors.codes import BizError, ErrCode

router = APIRouter(prefix="/api/devices", tags=["devices"])


@router.get("", response_model=ApiResponse)
async def list_devices(
    offset: int = Query(0, ge=0),
    limit: int = Query(50, ge=1, le=500),
    online_only: bool = Query(False),
    q: str | None = Query(None, max_length=100),
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        rows, total = await devices_repo.list_devices(
            session, offset=offset, limit=limit, online_only=online_only, q=q
        )
    return ok(data={
        "total": total,
        "items": [DeviceOut.model_validate(d).model_dump() for d in rows],
    })


@router.get("/{dev_number}", response_model=ApiResponse)
async def get_device(
    dev_number: str,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
    if d is None:
        raise BizError(ErrCode.BAD_PARAM, "device not found")
    return ok(data=DeviceOut.model_validate(d).model_dump())


@router.post("", response_model=ApiResponse)
async def create_device(
    body: DeviceCreateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        existing = await devices_repo.get_by_dev_number(session, body.dev_number)
        if existing:
            raise BizError(ErrCode.BAD_PARAM, "dev_number already exists")
        d = await devices_repo.create_device(
            session, **body.model_dump(exclude_none=True), usr_group=user.usr_group
        )
    return ok(data=DeviceOut.model_validate(d).model_dump())


@router.put("/{dev_number}", response_model=ApiResponse)
async def update_device(
    dev_number: str,
    body: DeviceUpdateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    check_ca(user, bit=0x02)
    updates = body.model_dump(exclude_none=True)
    if not updates:
        raise BizError(ErrCode.BAD_PARAM, "no fields to update")
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
        if d is None:
            raise BizError(ErrCode.BAD_PARAM, "device not found")
        await devices_repo.update_device_fields(session, d, updates)
        from sqlalchemy import text
        await session.execute(
            text("UPDATE devices SET update_flag = 1 WHERE dev_number = :d"),
            {"d": dev_number},
        )
    return ok(data=DeviceOut.model_validate(d).model_dump())


@router.delete("/{dev_number}", response_model=ApiResponse)
async def delete_device(
    dev_number: str,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
        if d is None:
            raise BizError(ErrCode.BAD_PARAM, "device not found")
        await devices_repo.soft_delete(session, d)
    return ok(data={"deleted": dev_number})
```

- [ ] **Step 4：写 unit test（mock repo）**

```python
# ruisheng-api/tests/unit/api/test_devices_list.py
import fakeredis.aioredis
from fastapi.testclient import TestClient

from ruisheng_api.core.security import client_fingerprint, issue_access_token
from ruisheng_api.db.repositories import devices as devices_repo
from ruisheng_api.deps import get_redis, get_session
from ruisheng_api.main import create_app


def _env(m):
    m.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_REDIS_URL", "redis://:p@h/0")
    m.setenv("API_JWT_SECRET", "x" * 64)


class _FakeDev:
    def __init__(self, **kw):
        self.__dict__.update(kw)


def _install(app, monkeypatch, rows):
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r

    class _S:
        async def begin(self): return self
        async def __aenter__(self): return self
        async def __aexit__(self, *a): pass
        async def execute(self, *a, **kw): return None

    async def fake_session():
        yield _S()

    app.dependency_overrides[get_session] = fake_session

    async def fake_list(session, **kw):
        return rows, len(rows)

    async def fake_apply(session, usr_group, role):
        return None

    monkeypatch.setattr(devices_repo, "list_devices", fake_list)
    from ruisheng_api.api import devices as devapi
    monkeypatch.setattr(devapi, "apply_tenant_context", fake_apply)
    return r


def _token(role="User"):
    fp = client_fingerprint("testclient", "testclient")
    return issue_access_token("alice", "g1", role, 0, fp, secret="x"*64, ttl_sec=900)


def test_list_devices_empty(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    _install(app, monkeypatch, [])
    resp = TestClient(app).get(
        "/api/devices", headers={"Authorization": f"Bearer {_token()}"}
    )
    assert resp.status_code == 200
    d = resp.json()["data"]
    assert d == {"total": 0, "items": []}


def test_list_devices_requires_auth(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    assert TestClient(app).get("/api/devices").status_code == 401
```

- [ ] **Step 5：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): D1 devices list + get + CRUD with tenant RLS

- GET /api/devices: offset/limit/online_only/q, apply_tenant_context first
- GET /api/devices/{dev_number}
- POST /api/devices: require role Company+, BAD_PARAM on dup
- PUT /api/devices/{dev_number}: role Company+ + CA bit 0x02 (config权),
  sets update_flag=1 for gw config watcher
- DELETE: soft delete via deleted_at
- devices_repo: list_devices/get_by_dev_number/create/update/soft_delete"
```

### Task D2: GET /api/devices/{dev_number}/realtime + GET /api/devices/{dev_number}/history

**Files:**
- Modify: `ruisheng-api/src/ruisheng_api/api/devices.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/timeseries.py`
- Test: `ruisheng-api/tests/unit/api/test_devices_realtime_history.py`

**设计**：
- `/realtime`：返回所有 point_data_realtime，带点位名 JOIN
- `/history`：强制分页（≤5000 行），>1d 自动降采样（响应头 X-Downsampled: true + X-Sample-Interval-S）

- [ ] **Step 1：timeseries repo**

```python
# ruisheng-api/src/ruisheng_api/db/repositories/timeseries.py
from __future__ import annotations
from datetime import datetime
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession


MAX_HISTORY_ROWS = 5000
SAMPLE_TIERS: tuple[tuple[int, int], ...] = (
    (86_400, 1),          # <=1d → raw
    (86_400 * 7, 300),    # <=7d → 5min
    (86_400 * 30, 3600),  # <=30d → 1h
    (10**9, 86_400),      # >30d → 1d
)


def pick_sample_interval(duration_sec: int) -> int:
    for threshold, interval in SAMPLE_TIERS:
        if duration_sec <= threshold:
            return interval
    return 86_400


async def load_realtime(session: AsyncSession, dev_number: str) -> list[dict]:
    stmt = text("""
        SELECT r.dev_number, r.point_id, r.org_value, r.rt_value, r.recorded_at,
               p.point_name, p.user_point_name, p.point_unit
        FROM point_data_realtime r
        LEFT JOIN device_points p ON p.id = r.point_id
        WHERE r.dev_number = :d
        ORDER BY r.point_id
    """)
    result = await session.execute(stmt, {"d": dev_number})
    return [dict(row._mapping) for row in result]


async def load_history(
    session: AsyncSession, *, dev_number: str, point_id: int | None,
    from_ts: datetime, to_ts: datetime, sample_interval_s: int,
    offset: int, limit: int,
) -> list[dict]:
    if sample_interval_s <= 1:
        sql = text("""
            SELECT dev_number, point_id, org_value, rt_value, recorded_at
            FROM point_data_history
            WHERE dev_number = :d
              AND (:p IS NULL OR point_id = :p)
              AND recorded_at >= :f AND recorded_at < :t
            ORDER BY recorded_at ASC
            OFFSET :o LIMIT :l
        """)
    else:
        sql = text("""
            SELECT dev_number, point_id,
                   avg(org_value) AS org_value,
                   avg(rt_value)  AS rt_value,
                   time_bucket(make_interval(secs => :s), recorded_at) AS recorded_at
            FROM point_data_history
            WHERE dev_number = :d
              AND (:p IS NULL OR point_id = :p)
              AND recorded_at >= :f AND recorded_at < :t
            GROUP BY dev_number, point_id, recorded_at
            ORDER BY recorded_at ASC
            OFFSET :o LIMIT :l
        """)
    result = await session.execute(sql, {
        "d": dev_number, "p": point_id, "f": from_ts, "t": to_ts,
        "s": sample_interval_s, "o": offset, "l": limit,
    })
    return [dict(row._mapping) for row in result]
```

- [ ] **Step 2：api 补 endpoint**

```python
# 补 api/devices.py
from datetime import datetime
from fastapi import Response
from ..db.repositories import timeseries as ts_repo


@router.get("/{dev_number}/realtime", response_model=ApiResponse)
async def realtime(
    dev_number: str,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
        if d is None:
            raise BizError(ErrCode.BAD_PARAM, "device not found")
        rows = await ts_repo.load_realtime(session, dev_number)
    return ok(data={"dev_number": dev_number, "points": rows})


@router.get("/{dev_number}/history", response_model=ApiResponse)
async def history(
    dev_number: str,
    response: Response,
    from_ts: datetime = Query(..., alias="from"),
    to_ts: datetime = Query(..., alias="to"),
    point_id: int | None = Query(None),
    sample_interval_s: int | None = Query(None, ge=1, le=86_400),
    offset: int = Query(0, ge=0),
    limit: int = Query(1000, ge=1, le=ts_repo.MAX_HISTORY_ROWS),
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    if to_ts <= from_ts:
        raise BizError(ErrCode.BAD_PARAM, "to must be > from")
    duration = int((to_ts - from_ts).total_seconds())
    interval = sample_interval_s or ts_repo.pick_sample_interval(duration)
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
        if d is None:
            raise BizError(ErrCode.BAD_PARAM, "device not found")
        rows = await ts_repo.load_history(
            session, dev_number=dev_number, point_id=point_id,
            from_ts=from_ts, to_ts=to_ts,
            sample_interval_s=interval, offset=offset, limit=limit,
        )
    if interval > 1:
        response.headers["X-Downsampled"] = "true"
        response.headers["X-Sample-Interval-S"] = str(interval)
    return ok(data={"rows": rows, "next_offset": offset + len(rows) if len(rows) == limit else None})
```

- [ ] **Step 3：写 unit test（pick_sample_interval + mock load_history）**

```python
# ruisheng-api/tests/unit/api/test_devices_realtime_history.py
from ruisheng_api.db.repositories.timeseries import pick_sample_interval


def test_pick_sample_interval_under_1d():
    assert pick_sample_interval(3600) == 1


def test_pick_sample_interval_7d():
    assert pick_sample_interval(86400 * 7) == 300


def test_pick_sample_interval_30d():
    assert pick_sample_interval(86400 * 30) == 3600


def test_pick_sample_interval_beyond_30d():
    assert pick_sample_interval(86400 * 365) == 86400
```

- [ ] **Step 4：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): D2 realtime + history endpoints + downsampling

- GET /api/devices/{dev_number}/realtime: JOIN point_data_realtime × device_points
- GET /api/devices/{dev_number}/history: strict pagination (≤5000),
  auto downsample via pick_sample_interval (§3.8.8)
- response headers X-Downsampled/X-Sample-Interval-S on downsample
- ts_repo uses TimescaleDB time_bucket for aggregation"
```

### Task D3: POST /api/devices/{dev_number}/control（Redis Streams XADD）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/control.py`
- Create: `ruisheng-api/src/ruisheng_api/pubsub/publisher.py`
- Create: `ruisheng-api/src/ruisheng_api/api/control.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/control.py`
- Test: `ruisheng-api/tests/unit/pubsub/test_publisher.py`
- Test: `ruisheng-api/tests/unit/api/test_control.py`

- [ ] **Step 1：schema**

```python
# ruisheng-api/src/ruisheng_api/api/schemas/control.py
from __future__ import annotations
from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field


class ControlAction(BaseModel):
    model_config = ConfigDict(extra="forbid")
    fun_code: int = Field(..., ge=1, le=100)
    reg: int = Field(..., ge=0, le=65535)
    value: int = Field(..., ge=-2**31, le=2**31 - 1)
    high_risk: bool = False


class ControlResponseData(BaseModel):
    cmd_id: str
    status: str
    acted_at: datetime
```

- [ ] **Step 2：publisher.py**

```python
"""pubsub publisher：XADD stream:control:cmd 等。"""

from __future__ import annotations

import json

import redis.asyncio as redis_async


STREAM_CONTROL_CMD = "stream:control:cmd"


async def xadd_control_cmd(r: redis_async.Redis, *, cmd_id: str, payload: dict) -> str:
    """返回 Redis stream entry id。MAXLEN ~ 50000 防爆。"""
    return await r.xadd(
        STREAM_CONTROL_CMD,
        {"cmd_id": cmd_id, "payload": json.dumps(payload, ensure_ascii=False)},
        maxlen=50_000, approximate=True,
    )
```

- [ ] **Step 3：control repo**

```python
# ruisheng-api/src/ruisheng_api/db/repositories/control.py
from __future__ import annotations
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession


async def insert_action(
    session: AsyncSession, *, dev_number: str, user_name: str,
    cmd_id: str, action: dict, usr_group: str,
) -> int:
    import json
    sql = text("""
        INSERT INTO user_control_actions (dev_number, user_name, action, cmd_id, result, usr_group)
        VALUES (:d, :u, CAST(:a AS JSONB), :c, 'pending', :g)
        RETURNING id
    """)
    row = await session.execute(sql, {
        "d": dev_number, "u": user_name, "a": json.dumps(action),
        "c": cmd_id, "g": usr_group,
    })
    return int(row.scalar_one())


async def check_recent_duplicate(
    session: AsyncSession, *, user_name: str, dev_number: str,
    action_key: str, seconds: int = 5,
) -> bool:
    sql = text("""
        SELECT 1 FROM user_control_actions
        WHERE user_name = :u AND dev_number = :d
          AND action->>'action_key' = :k
          AND acted_at > now() - make_interval(secs => :s)
        LIMIT 1
    """)
    return (await session.execute(sql, {
        "u": user_name, "d": dev_number, "k": action_key, "s": seconds,
    })).scalar_one_or_none() is not None
```

- [ ] **Step 4：control api**

```python
"""Control API：POST /api/devices/{dev_number}/control。"""

from __future__ import annotations

import hashlib
from datetime import datetime, timezone

import redis.asyncio as redis_async
import ulid
from fastapi import APIRouter, Depends, Header
from loguru import logger
from sqlalchemy.ext.asyncio import AsyncSession

from ..core.rbac import CurrentUser, check_ca
from ..core.response import ApiResponse, ok
from ..core.tenant import apply_tenant_context
from ..db.repositories import control as control_repo
from ..db.repositories import devices as devices_repo
from ..deps import get_current_user, get_redis, get_session
from ..pubsub.publisher import xadd_control_cmd
from ..services import otp as otp_svc
from .schemas.control import ControlAction, ControlResponseData
from ruisheng_shared.errors.codes import BizError, ErrCode

router = APIRouter(prefix="/api/devices", tags=["control"])


def _action_key(a: ControlAction) -> str:
    return hashlib.sha256(f"{a.fun_code}:{a.reg}:{a.value}".encode()).hexdigest()[:16]


@router.post("/{dev_number}/control", response_model=ApiResponse)
async def control(
    dev_number: str,
    body: ControlAction,
    idempotency_key: str | None = Header(default=None, alias="Idempotency-Key"),
    otp_code: str | None = Header(default=None, alias="X-OTP-Code"),
    user: CurrentUser = Depends(get_current_user),
    r: redis_async.Redis = Depends(get_redis),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_ca(user, bit=0x01)  # 控制权
    if body.high_risk:
        check_ca(user, bit=0x04)
        if not otp_code or not await otp_svc.verify_otp(
            r, action="control", key=user.user_name, code=otp_code
        ):
            raise BizError(ErrCode.UNAUTHED, "OTP required for high-risk op")

    if idempotency_key:
        cached = await r.get(f"idempotency:control:{user.user_name}:{idempotency_key}")
        if cached:
            return ok(data={"cmd_id": cached, "status": "duplicate"})

    cmd_id = str(ulid.new())
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
        if d is None:
            raise BizError(ErrCode.BAD_PARAM, "device not found")
        if await control_repo.check_recent_duplicate(
            session, user_name=user.user_name, dev_number=dev_number,
            action_key=_action_key(body),
        ):
            raise BizError(ErrCode.BIZ_FAIL, "duplicate action within 5s")
        action_dict = {**body.model_dump(), "action_key": _action_key(body)}
        await control_repo.insert_action(
            session, dev_number=dev_number, user_name=user.user_name,
            cmd_id=cmd_id, action=action_dict, usr_group=user.usr_group,
        )

    await xadd_control_cmd(r, cmd_id=cmd_id, payload={
        "dev_number": dev_number, "user_name": user.user_name, **body.model_dump(),
    })

    if idempotency_key:
        await r.setex(
            f"idempotency:control:{user.user_name}:{idempotency_key}", 86400, cmd_id
        )

    logger.bind(cmd_id=cmd_id, dev_number=dev_number, user_name=user.user_name).info(
        "control cmd dispatched"
    )
    return ok(data=ControlResponseData(
        cmd_id=cmd_id, status="pending",
        acted_at=datetime.now(timezone.utc),
    ).model_dump(mode="json"))
```

- [ ] **Step 5：测试（mock session + fakeredis + mock repos）**

```python
# ruisheng-api/tests/unit/pubsub/test_publisher.py
import json
import fakeredis.aioredis
import pytest
from ruisheng_api.pubsub.publisher import STREAM_CONTROL_CMD, xadd_control_cmd


@pytest.mark.asyncio
async def test_xadd_control_cmd_writes_stream():
    r = fakeredis.aioredis.FakeRedis()
    await xadd_control_cmd(r, cmd_id="01HA", payload={"x": 1})
    entries = await r.xread({STREAM_CONTROL_CMD: "0-0"}, count=10)
    _, rows = entries[0]
    _, data = rows[0]
    d = {k.decode() if isinstance(k, bytes) else k:
         v.decode() if isinstance(v, bytes) else v for k, v in data.items()}
    assert d["cmd_id"] == "01HA"
    assert json.loads(d["payload"])["x"] == 1
```

```python
# ruisheng-api/tests/unit/api/test_control.py
import fakeredis.aioredis
from fastapi.testclient import TestClient

from ruisheng_api.core.security import client_fingerprint, issue_access_token
from ruisheng_api.db.repositories import control as control_repo
from ruisheng_api.db.repositories import devices as devices_repo
from ruisheng_api.deps import get_redis, get_session
from ruisheng_api.main import create_app


def _env(m):
    m.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_REDIS_URL", "redis://:p@h/0")
    m.setenv("API_JWT_SECRET", "x" * 64)


def _tok(ca):
    fp = client_fingerprint("testclient", "testclient")
    return issue_access_token("alice", "g1", "User", ca, fp, secret="x"*64, ttl_sec=900)


class _S:
    async def begin(self): return self
    async def __aenter__(self): return self
    async def __aexit__(self, *a): pass
    async def execute(self, *a, **kw): return None


def _install_happy(app, monkeypatch):
    r = fakeredis.aioredis.FakeRedis()
    app.dependency_overrides[get_redis] = lambda: r

    async def fake_session():
        yield _S()

    app.dependency_overrides[get_session] = fake_session

    async def fake_get(session, dev_number):
        return type("D", (), {"dev_number": dev_number, "usr_group": "g1"})()
    async def fake_dup(session, **kw): return False
    async def fake_ins(session, **kw): return 1
    async def fake_ctx(session, usr_group, role): return None
    monkeypatch.setattr(devices_repo, "get_by_dev_number", fake_get)
    monkeypatch.setattr(control_repo, "check_recent_duplicate", fake_dup)
    monkeypatch.setattr(control_repo, "insert_action", fake_ins)
    from ruisheng_api.api import control as ctrlapi
    monkeypatch.setattr(ctrlapi, "apply_tenant_context", fake_ctx)
    return r


def test_control_requires_ca_bit0(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    _install_happy(app, monkeypatch)
    resp = TestClient(app).post(
        "/api/devices/60270012/control",
        json={"fun_code": 6, "reg": 0, "value": 1},
        headers={"Authorization": f"Bearer {_tok(ca=0)}"},
    )
    assert resp.status_code == 403


def test_control_happy(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    _install_happy(app, monkeypatch)
    resp = TestClient(app).post(
        "/api/devices/60270012/control",
        json={"fun_code": 6, "reg": 0, "value": 1},
        headers={"Authorization": f"Bearer {_tok(ca=1)}"},
    )
    assert resp.status_code == 200, resp.text
    d = resp.json()["data"]
    assert d["status"] == "pending"
    assert len(d["cmd_id"]) == 26


def test_control_high_risk_requires_otp(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    _install_happy(app, monkeypatch)
    resp = TestClient(app).post(
        "/api/devices/60270012/control",
        json={"fun_code": 6, "reg": 0, "value": 1, "high_risk": True},
        headers={"Authorization": f"Bearer {_tok(ca=0x05)}"},
    )
    assert resp.status_code == 401
```

- [ ] **Step 6：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): D3 POST /api/devices/{d}/control via Redis Streams

- ControlAction schema: fun_code/reg/value/high_risk
- check_ca 0x01; high_risk → check_ca 0x04 + X-OTP-Code verify
- 5s duplicate guard (same user+dev+action_key via control_repo)
- Idempotency-Key header → 24h cached cmd_id
- XADD stream:control:cmd with MAXLEN ~50000
- INSERT user_control_actions(pending) before XADD, same tx"
```

### Task D4: Control cancel + query（GET /api/control/commands + DELETE /{cmd_id}）

**Files:**
- Modify: `ruisheng-api/src/ruisheng_api/api/control.py`
- Modify: `ruisheng-api/src/ruisheng_api/db/repositories/control.py`
- Test: `ruisheng-api/tests/unit/api/test_control_query.py`

**设计**：
- `GET /api/control/commands`：列 `user_control_actions`，自己+本租户过滤
- `DELETE /api/control/commands/{cmd_id}`：仅 pending 可取消；UPDATE result='cancelled' + completed_at=now

- [ ] **Step 1：补 control_repo**

```python
async def list_actions(
    session: AsyncSession, *, user_name: str | None, dev_number: str | None,
    offset: int, limit: int,
) -> list[dict]:
    sql = text("""
        SELECT id, dev_number, user_name, action, cmd_id, result,
               acted_at, completed_at, usr_group
        FROM user_control_actions
        WHERE (:u IS NULL OR user_name = :u)
          AND (:d IS NULL OR dev_number = :d)
        ORDER BY acted_at DESC
        OFFSET :o LIMIT :l
    """)
    rows = await session.execute(sql, {
        "u": user_name, "d": dev_number, "o": offset, "l": limit,
    })
    return [dict(r._mapping) for r in rows]


async def cancel_action(session: AsyncSession, cmd_id: str) -> bool:
    sql = text("""
        UPDATE user_control_actions
        SET result = 'cancelled', completed_at = now()
        WHERE cmd_id = :c AND result = 'pending'
    """)
    res = await session.execute(sql, {"c": cmd_id})
    return (res.rowcount or 0) > 0
```

- [ ] **Step 2：补 api/control.py**

```python
query_router = APIRouter(prefix="/api/control", tags=["control"])


@query_router.get("/commands", response_model=ApiResponse)
async def list_commands(
    user_name: str | None = None,
    dev_number: str | None = None,
    offset: int = 0, limit: int = 50,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    effective_user = user_name if user.role in ("Administrators", "GroupCompany", "Company") else user.user_name
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        rows = await control_repo.list_actions(
            session, user_name=effective_user, dev_number=dev_number,
            offset=offset, limit=limit,
        )
    return ok(data={"items": rows})


@query_router.delete("/commands/{cmd_id}", response_model=ApiResponse)
async def cancel_command(
    cmd_id: str,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        ok_ = await control_repo.cancel_action(session, cmd_id)
    if not ok_:
        raise BizError(ErrCode.BAD_PARAM, "cmd not pending or not found")
    return ok(data={"cmd_id": cmd_id, "status": "cancelled"})
```

挂载：在 api/__init__.py 加 `app.include_router(control.query_router)`。

- [ ] **Step 3：写单测 + 跑 → PASS；commit**

### Task D5: Stage D tag + PROGRESS

tag `plan-2-stage-d-complete`；integration test 在 Stage M 收尾时做真 PG 端到端验证。

---

## Stage E: Points + Alarm configs + Alarm records

### Task E1: Points API（CRUD）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/points.py`
- Create: `ruisheng-api/src/ruisheng_api/api/points.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/points.py`
- Test: `ruisheng-api/tests/unit/api/test_points.py`

- [ ] **Step 1：schemas/points.py**

```python
from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field


class PointOut(BaseModel):
    model_config = ConfigDict(from_attributes=True, extra="ignore")
    id: int
    dev_number: str
    point_name: str
    user_point_name: str | None
    point_number: int
    fun_code: int
    dev_addr: int
    r_bit: int | None
    value_type: str
    point_unit: str | None
    point_ratio: float
    point_offset: float
    user_ratio: float
    user_point_offset: float
    min_value: float | None
    max_value: float | None
    show: int


class PointCreateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    point_name: str = Field(..., min_length=1, max_length=100)
    user_point_name: str | None = Field(default=None, max_length=100)
    point_number: int = Field(..., ge=0, le=65535)
    fun_code: int = Field(..., ge=1, le=4)
    dev_addr: int = Field(..., ge=1, le=247)
    r_bit: int | None = None
    value_type: str = Field(..., pattern=r"^(字|双字|bit)$")
    point_unit: str | None = Field(default=None, max_length=20)
    point_ratio: float = 1.0
    point_offset: float = 0.0
    user_ratio: float = 1.0
    user_point_offset: float = 0.0
    min_value: float | None = None
    max_value: float | None = None
    show: int = Field(default=1, ge=0, le=1)


class PointUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    user_point_name: str | None = None
    point_unit: str | None = None
    point_ratio: float | None = None
    point_offset: float | None = None
    user_ratio: float | None = None
    user_point_offset: float | None = None
    min_value: float | None = None
    max_value: float | None = None
    show: int | None = None
```

- [ ] **Step 2：repositories/points.py**

```python
from __future__ import annotations
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from ruisheng_shared.models.devices import DevicePoint


async def list_points(session: AsyncSession, dev_number: str) -> list[DevicePoint]:
    stmt = select(DevicePoint).where(DevicePoint.dev_number == dev_number).order_by(DevicePoint.id)
    return list((await session.execute(stmt)).scalars())


async def get_point(session: AsyncSession, point_id: int) -> DevicePoint | None:
    return (await session.execute(
        select(DevicePoint).where(DevicePoint.id == point_id)
    )).scalar_one_or_none()


async def create_point(session: AsyncSession, *, dev_number: str, **fields) -> DevicePoint:
    p = DevicePoint(dev_number=dev_number, **fields)
    session.add(p)
    await session.flush()
    return p


async def update_point(session: AsyncSession, point: DevicePoint, updates: dict) -> DevicePoint:
    for k, v in updates.items():
        setattr(point, k, v)
    await session.flush()
    return point


async def delete_point(session: AsyncSession, point: DevicePoint) -> None:
    await session.delete(point)
    await session.flush()
```

- [ ] **Step 3：api/points.py**

```python
"""Points API：嵌套在 /api/devices/{dev_number}/points/*。"""

from __future__ import annotations

from fastapi import APIRouter, Depends
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from ..core.rbac import CurrentUser, check_ca, check_role
from ..core.response import ApiResponse, ok
from ..core.tenant import apply_tenant_context
from ..db.repositories import devices as devices_repo
from ..db.repositories import points as points_repo
from ..deps import get_current_user, get_session
from .schemas.points import PointCreateRequest, PointOut, PointUpdateRequest
from ruisheng_shared.errors.codes import BizError, ErrCode

router = APIRouter(prefix="/api/devices", tags=["points"])


async def _require_dev(session, dev_number, user):
    await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
    d = await devices_repo.get_by_dev_number(session, dev_number)
    if d is None:
        raise BizError(ErrCode.BAD_PARAM, "device not found")
    return d


async def _bump_flag(session, dev_number):
    await session.execute(
        text("UPDATE devices SET update_flag = 1 WHERE dev_number = :d"),
        {"d": dev_number},
    )


@router.get("/{dev_number}/points", response_model=ApiResponse)
async def list_points(
    dev_number: str,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await _require_dev(session, dev_number, user)
        rows = await points_repo.list_points(session, dev_number)
    return ok(data={"items": [PointOut.model_validate(p).model_dump() for p in rows]})


@router.post("/{dev_number}/points", response_model=ApiResponse)
async def create_point(
    dev_number: str,
    body: PointCreateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    check_ca(user, bit=0x02)
    async with session.begin():
        await _require_dev(session, dev_number, user)
        p = await points_repo.create_point(
            session, dev_number=dev_number, **body.model_dump(exclude_none=True)
        )
        await _bump_flag(session, dev_number)
    return ok(data=PointOut.model_validate(p).model_dump())


@router.put("/{dev_number}/points/{point_id}", response_model=ApiResponse)
async def update_point(
    dev_number: str,
    point_id: int,
    body: PointUpdateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    check_ca(user, bit=0x02)
    updates = body.model_dump(exclude_none=True)
    if not updates:
        raise BizError(ErrCode.BAD_PARAM, "no fields to update")
    async with session.begin():
        await _require_dev(session, dev_number, user)
        p = await points_repo.get_point(session, point_id)
        if p is None or p.dev_number != dev_number:
            raise BizError(ErrCode.BAD_PARAM, "point not found")
        await points_repo.update_point(session, p, updates)
        await _bump_flag(session, dev_number)
    return ok(data=PointOut.model_validate(p).model_dump())


@router.delete("/{dev_number}/points/{point_id}", response_model=ApiResponse)
async def delete_point(
    dev_number: str,
    point_id: int,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    async with session.begin():
        await _require_dev(session, dev_number, user)
        p = await points_repo.get_point(session, point_id)
        if p is None or p.dev_number != dev_number:
            raise BizError(ErrCode.BAD_PARAM, "point not found")
        await points_repo.delete_point(session, p)
        await _bump_flag(session, dev_number)
    return ok(data={"deleted": point_id})
```

- [ ] **Step 4：写单测（mock repo）类似 test_devices_list 结构**

- [ ] **Step 5：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): E1 points CRUD nested under /api/devices/{d}/points

- PointOut/PointCreateRequest/PointUpdateRequest schemas
- role Company+ + CA 0x02 for mutations
- all mutations bump devices.update_flag (gw config watcher)
- 4 endpoints: list/create/update/delete"
```

### Task E2: Alarm configs API（device_waring_cfgs CRUD）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/alarms.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/alarms.py`
- Modify: `ruisheng-api/src/ruisheng_api/api/alarms.py`（新建）
- Test: `ruisheng-api/tests/unit/api/test_alarm_configs.py`

**设计**：
- `GET /api/devices/{dev_number}/alarms/configs` 列告警阈值
- `POST/PUT/DELETE` 改阈值（role Company+ + CA 0x02，且 bump update_flag）

- [ ] **Step 1：schemas/alarms.py**

```python
from __future__ import annotations
from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field


class AlarmCfgOut(BaseModel):
    model_config = ConfigDict(from_attributes=True, extra="ignore")
    id: int
    dev_number: str
    point_id: int
    reg_bit: int | None
    alarm_name: str
    alarm_type: str
    limit_value: float
    relation_point_id: int | None
    relation_reg_bit: int | None
    relation_alarm_type: str | None
    relation_limit_value: float | None
    enable: bool
    phone_alarm: int
    reset_remind: bool
    waring_flag: bool
    alarm_msg: str | None


class AlarmCfgCreateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    point_id: int
    reg_bit: int | None = None
    alarm_name: str = Field(..., max_length=100)
    alarm_type: str = Field(..., pattern=r"^(>|<|=|!=|LX)$")
    limit_value: float
    relation_point_id: int | None = None
    relation_reg_bit: int | None = None
    relation_alarm_type: str | None = Field(default=None, pattern=r"^(>|<|=|!=|LX)$")
    relation_limit_value: float | None = None
    enable: bool = True
    phone_alarm: int = Field(default=0, ge=0)
    reset_remind: bool = False
    alarm_msg: str | None = Field(default=None, max_length=255)


class AlarmCfgUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    alarm_name: str | None = None
    alarm_type: str | None = Field(default=None, pattern=r"^(>|<|=|!=|LX)$")
    limit_value: float | None = None
    enable: bool | None = None
    phone_alarm: int | None = None
    reset_remind: bool | None = None
    alarm_msg: str | None = None


class AlarmRecordOut(BaseModel):
    model_config = ConfigDict(from_attributes=True, extra="ignore")
    id: int
    dev_number: str
    point_id: int | None
    alarm_name: str | None
    alarm_msg: str | None
    alarm_value: float | None
    channels_sent: dict
    triggered_at: datetime
    reset_at: datetime | None
```

- [ ] **Step 2：repositories/alarms.py**

```python
from __future__ import annotations
from datetime import datetime, timezone
from sqlalchemy import select, text
from sqlalchemy.ext.asyncio import AsyncSession
from ruisheng_shared.models.alarms import DeviceWaringCfg, AlarmRecord


async def list_cfgs(session: AsyncSession, dev_number: str) -> list[DeviceWaringCfg]:
    stmt = select(DeviceWaringCfg).where(DeviceWaringCfg.dev_number == dev_number)
    return list((await session.execute(stmt)).scalars())


async def get_cfg(session: AsyncSession, cfg_id: int) -> DeviceWaringCfg | None:
    return (await session.execute(
        select(DeviceWaringCfg).where(DeviceWaringCfg.id == cfg_id)
    )).scalar_one_or_none()


async def create_cfg(session, *, dev_number: str, **fields) -> DeviceWaringCfg:
    c = DeviceWaringCfg(dev_number=dev_number, **fields)
    session.add(c)
    await session.flush()
    return c


async def update_cfg(session, cfg: DeviceWaringCfg, updates: dict) -> DeviceWaringCfg:
    for k, v in updates.items():
        setattr(cfg, k, v)
    await session.flush()
    return cfg


async def delete_cfg(session, cfg: DeviceWaringCfg) -> None:
    await session.delete(cfg)
    await session.flush()


async def list_records(
    session, *, dev_number: str | None, active_only: bool,
    from_ts: datetime | None, to_ts: datetime | None,
    offset: int, limit: int,
) -> list[dict]:
    sql = text("""
        SELECT id, dev_number, point_id, alarm_name, alarm_msg, alarm_value,
               channels_sent, triggered_at, reset_at, usr_group
        FROM alarm_records
        WHERE (:d IS NULL OR dev_number = :d)
          AND (:active = false OR reset_at IS NULL)
          AND (:f IS NULL OR triggered_at >= :f)
          AND (:t IS NULL OR triggered_at < :t)
        ORDER BY triggered_at DESC
        OFFSET :o LIMIT :l
    """)
    rows = await session.execute(sql, {
        "d": dev_number, "active": active_only, "f": from_ts, "t": to_ts,
        "o": offset, "l": limit,
    })
    return [dict(r._mapping) for r in rows]


async def reset_alarm(session, alarm_id: int) -> bool:
    sql = text("""
        UPDATE alarm_records
        SET reset_at = now()
        WHERE id = :i AND reset_at IS NULL
    """)
    res = await session.execute(sql, {"i": alarm_id})
    return (res.rowcount or 0) > 0
```

- [ ] **Step 3：api/alarms.py（cfg + records）**

```python
"""Alarms API：/api/alarms/* + nested configs。"""

from __future__ import annotations

from datetime import datetime

from fastapi import APIRouter, Depends, Query
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from ..core.rbac import CurrentUser, check_ca, check_role
from ..core.response import ApiResponse, ok
from ..core.tenant import apply_tenant_context
from ..db.repositories import alarms as alarms_repo
from ..db.repositories import devices as devices_repo
from ..deps import get_current_user, get_session
from .schemas.alarms import (
    AlarmCfgCreateRequest, AlarmCfgOut, AlarmCfgUpdateRequest, AlarmRecordOut,
)
from ruisheng_shared.errors.codes import BizError, ErrCode

cfg_router = APIRouter(prefix="/api/devices", tags=["alarms"])
record_router = APIRouter(prefix="/api/alarms", tags=["alarms"])


@cfg_router.get("/{dev_number}/alarms/configs", response_model=ApiResponse)
async def list_configs(
    dev_number: str,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
        if d is None:
            raise BizError(ErrCode.BAD_PARAM, "device not found")
        rows = await alarms_repo.list_cfgs(session, dev_number)
    return ok(data={"items": [AlarmCfgOut.model_validate(c).model_dump() for c in rows]})


@cfg_router.post("/{dev_number}/alarms/configs", response_model=ApiResponse)
async def create_config(
    dev_number: str, body: AlarmCfgCreateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    check_ca(user, bit=0x02)
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        d = await devices_repo.get_by_dev_number(session, dev_number)
        if d is None:
            raise BizError(ErrCode.BAD_PARAM, "device not found")
        c = await alarms_repo.create_cfg(
            session, dev_number=dev_number, **body.model_dump(exclude_none=True)
        )
        await session.execute(
            text("UPDATE devices SET update_flag = 1 WHERE dev_number = :d"),
            {"d": dev_number},
        )
    return ok(data=AlarmCfgOut.model_validate(c).model_dump())


@cfg_router.put("/{dev_number}/alarms/configs/{cfg_id}", response_model=ApiResponse)
async def update_config(
    dev_number: str, cfg_id: int, body: AlarmCfgUpdateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    check_ca(user, bit=0x02)
    updates = body.model_dump(exclude_none=True)
    if not updates:
        raise BizError(ErrCode.BAD_PARAM, "no fields")
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        c = await alarms_repo.get_cfg(session, cfg_id)
        if c is None or c.dev_number != dev_number:
            raise BizError(ErrCode.BAD_PARAM, "cfg not found")
        await alarms_repo.update_cfg(session, c, updates)
        await session.execute(
            text("UPDATE devices SET update_flag = 1 WHERE dev_number = :d"),
            {"d": dev_number},
        )
    return ok(data=AlarmCfgOut.model_validate(c).model_dump())


@cfg_router.delete("/{dev_number}/alarms/configs/{cfg_id}", response_model=ApiResponse)
async def delete_config(
    dev_number: str, cfg_id: int,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    check_ca(user, bit=0x02)
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        c = await alarms_repo.get_cfg(session, cfg_id)
        if c is None or c.dev_number != dev_number:
            raise BizError(ErrCode.BAD_PARAM, "cfg not found")
        await alarms_repo.delete_cfg(session, c)
        await session.execute(
            text("UPDATE devices SET update_flag = 1 WHERE dev_number = :d"),
            {"d": dev_number},
        )
    return ok(data={"deleted": cfg_id})


@record_router.get("", response_model=ApiResponse)
async def list_records(
    dev_number: str | None = Query(None),
    active_only: bool = Query(False),
    from_ts: datetime | None = Query(None, alias="from"),
    to_ts: datetime | None = Query(None, alias="to"),
    offset: int = Query(0, ge=0),
    limit: int = Query(50, ge=1, le=500),
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        rows = await alarms_repo.list_records(
            session, dev_number=dev_number, active_only=active_only,
            from_ts=from_ts, to_ts=to_ts, offset=offset, limit=limit,
        )
    return ok(data={"items": rows})


@record_router.put("/{alarm_id}/reset", response_model=ApiResponse)
async def reset_alarm(
    alarm_id: int,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        if not await alarms_repo.reset_alarm(session, alarm_id):
            raise BizError(ErrCode.BAD_PARAM, "alarm not found or already reset")
    return ok(data={"alarm_id": alarm_id, "reset": True})
```

挂载到 api/__init__.py：`app.include_router(alarms.cfg_router)` + `app.include_router(alarms.record_router)`。

- [ ] **Step 4：写单测 + 跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): E2 alarm configs CRUD + alarm records list/reset

- GET/POST/PUT/DELETE /api/devices/{d}/alarms/configs (role+CA 0x02)
- all mutations bump devices.update_flag
- GET /api/alarms: filters dev_number/active_only/from/to; tenant RLS
- PUT /api/alarms/{id}/reset: UPDATE reset_at=now() where reset_at IS NULL"
```

### Task E3: Stage E tag + PROGRESS

tag `plan-2-stage-e-complete`。

---

## Stage F: Redis Streams Consumer + WebSocket

### Task F1: pubsub/alarm_consumer.py（XREADGROUP alarm:fired → DB + 广播）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/pubsub/alarm_consumer.py`
- Create: `ruisheng-api/src/ruisheng_api/pubsub/ws_manager.py`
- Test: `ruisheng-api/tests/unit/pubsub/test_alarm_consumer.py`
- Test: `ruisheng-api/tests/unit/pubsub/test_ws_manager.py`

**设计**：
- consumer 读 `stream:alarm:fired`，消费组 `api-alarm-consumer`
- `XAUTOCLAIM` 先取回崩溃时未 ACK 的 entry
- 成功处理（WS 广播 + 通知 stub Stage J 接入）→ XACK；5 次失败 → DLQ
- 幂等：`event_id` SET NX EX 86400 去重

- [ ] **Step 1：ws_manager.py**

```python
"""WebSocket 连接管理。每连接独立 asyncio.Queue(maxsize=500)。"""

from __future__ import annotations

import asyncio
import json
from dataclasses import dataclass, field
from loguru import logger
from starlette.websockets import WebSocket


@dataclass
class WSClient:
    ws: WebSocket
    user_name: str
    usr_group: str
    role: str
    queue: asyncio.Queue = field(default_factory=lambda: asyncio.Queue(maxsize=500))


class WSManager:
    def __init__(self) -> None:
        self._clients: set[WSClient] = set()
        self._lock = asyncio.Lock()

    async def add(self, client: WSClient) -> None:
        async with self._lock:
            self._clients.add(client)

    async def remove(self, client: WSClient) -> None:
        async with self._lock:
            self._clients.discard(client)

    async def clients_snapshot(self) -> list[WSClient]:
        async with self._lock:
            return list(self._clients)

    async def broadcast(self, message: dict, *, tenant_filter: str | None = None) -> int:
        """广播到租户匹配的所有连接。返回成功入队数。"""
        payload = json.dumps(message, ensure_ascii=False)
        count = 0
        for c in await self.clients_snapshot():
            if tenant_filter and c.role != "Administrators" and c.usr_group != tenant_filter:
                continue
            try:
                c.queue.put_nowait(payload)
                count += 1
            except asyncio.QueueFull:
                # 丢老：§3.8.4 WS 慢消费者策略
                try:
                    c.queue.get_nowait()
                except asyncio.QueueEmpty:
                    pass
                try:
                    c.queue.put_nowait(payload)
                    count += 1
                except asyncio.QueueFull:
                    logger.warning("WS queue overflow dropping message")
        return count
```

- [ ] **Step 2：alarm_consumer.py**

```python
"""alarm:fired XREADGROUP consumer。"""

from __future__ import annotations

import asyncio
import json
from dataclasses import dataclass
from typing import Any

import redis.asyncio as redis_async
from loguru import logger

from .ws_manager import WSManager

STREAM_ALARM_FIRED = "stream:alarm:fired"
GROUP_API = "api-alarm-consumer"
DLQ_ALARM = "stream:dlq:alarm"
CONSUMER_NAME_PREFIX = "api-"


@dataclass
class AlarmConsumerConfig:
    consumer_name: str
    block_ms: int = 5_000
    batch: int = 20
    max_retries: int = 5


async def ensure_group(r: redis_async.Redis, stream: str, group: str) -> None:
    try:
        await r.xgroup_create(stream, group, id="0", mkstream=True)
    except redis_async.ResponseError as e:  # type: ignore[attr-defined]
        if "BUSYGROUP" not in str(e):
            raise


async def _handle_one(
    r: redis_async.Redis, entry: dict[str, Any], ws: WSManager,
) -> bool:
    event_id = str(entry.get("event_id") or "")
    if not event_id:
        return False
    ok_ = await r.set(f"alarm_seen:{event_id}", "1", nx=True, ex=86_400)
    if not ok_:
        return True
    payload = {
        "type": "alarm",
        "event_id": int(event_id),
        "dev_number": str(entry.get("dev_number") or ""),
        "alarm_name": str(entry.get("alarm_name") or ""),
        "value": float(entry.get("value") or 0),
        "limit": float(entry.get("limit") or 0),
        "ts": str(entry.get("ts") or ""),
    }
    tenant = str(entry.get("usr_group") or "")
    await ws.broadcast(payload, tenant_filter=tenant or None)
    # Stage J 会在此接入 fan-out（wechat/sms/email）
    return True


async def consume_once(
    r: redis_async.Redis, cfg: AlarmConsumerConfig, ws: WSManager,
) -> int:
    # 先 claim 未 ack 的
    claimed = await r.xautoclaim(
        STREAM_ALARM_FIRED, GROUP_API, cfg.consumer_name,
        min_idle_time=30_000, count=cfg.batch,
    )
    consumed = 0
    _, items, _ = claimed
    for msg_id, fields in items:
        try:
            if await _handle_one(r, fields, ws):
                await r.xack(STREAM_ALARM_FIRED, GROUP_API, msg_id)
            consumed += 1
        except Exception:
            logger.exception("claimed entry handle failed, leaving in PEL")

    # 新读
    read = await r.xreadgroup(
        GROUP_API, cfg.consumer_name,
        {STREAM_ALARM_FIRED: ">"}, count=cfg.batch, block=cfg.block_ms,
    )
    if not read:
        return consumed
    for _stream, msgs in read:
        for msg_id, fields in msgs:
            try:
                if await _handle_one(r, fields, ws):
                    await r.xack(STREAM_ALARM_FIRED, GROUP_API, msg_id)
                else:
                    await r.xadd(DLQ_ALARM, fields)
                    await r.xack(STREAM_ALARM_FIRED, GROUP_API, msg_id)
                consumed += 1
            except Exception:
                logger.exception("alarm handle failed msg_id=%s", msg_id)
    return consumed


async def consumer_loop(
    r: redis_async.Redis, cfg: AlarmConsumerConfig, ws: WSManager,
    stop_event: asyncio.Event,
) -> None:
    await ensure_group(r, STREAM_ALARM_FIRED, GROUP_API)
    while not stop_event.is_set():
        try:
            await consume_once(r, cfg, ws)
        except Exception:
            logger.exception("consume_once crashed; retry in 2s")
            await asyncio.sleep(2)
```

- [ ] **Step 3：写测试（fakeredis；consumer 消一条后 XACK）**

```python
# ruisheng-api/tests/unit/pubsub/test_ws_manager.py
import asyncio
import pytest
from ruisheng_api.pubsub.ws_manager import WSClient, WSManager


class _WS:
    pass


@pytest.mark.asyncio
async def test_broadcast_respects_tenant():
    m = WSManager()
    a = WSClient(ws=_WS(), user_name="a", usr_group="g1", role="User")
    b = WSClient(ws=_WS(), user_name="b", usr_group="g2", role="User")
    admin = WSClient(ws=_WS(), user_name="c", usr_group="", role="Administrators")
    await m.add(a); await m.add(b); await m.add(admin)
    n = await m.broadcast({"x": 1}, tenant_filter="g1")
    assert n == 2  # a + admin


@pytest.mark.asyncio
async def test_broadcast_drops_oldest_when_full():
    m = WSManager()
    c = WSClient(ws=_WS(), user_name="a", usr_group="g1", role="User")
    c.queue = asyncio.Queue(maxsize=1)
    c.queue.put_nowait("old")
    await m.add(c)
    await m.broadcast({"x": 2})
    msg = c.queue.get_nowait()
    assert "2" in msg
```

```python
# ruisheng-api/tests/unit/pubsub/test_alarm_consumer.py
import fakeredis.aioredis
import pytest
from ruisheng_api.pubsub.alarm_consumer import (
    AlarmConsumerConfig, STREAM_ALARM_FIRED, consume_once, ensure_group,
)
from ruisheng_api.pubsub.ws_manager import WSManager


@pytest.mark.asyncio
async def test_consume_once_broadcasts_and_acks():
    r = fakeredis.aioredis.FakeRedis(decode_responses=True)
    await ensure_group(r, STREAM_ALARM_FIRED, "api-alarm-consumer")
    await r.xadd(STREAM_ALARM_FIRED, {
        "event_id": "42", "dev_number": "60270012", "alarm_name": "overcurrent",
        "value": "95", "limit": "80", "ts": "2026-04-19T10:30:00Z",
        "usr_group": "g1",
    })
    ws = WSManager()
    cfg = AlarmConsumerConfig(consumer_name="t1", block_ms=100, batch=10)
    n = await consume_once(r, cfg, ws)
    assert n == 1
    pending = await r.xpending(STREAM_ALARM_FIRED, "api-alarm-consumer")
    assert pending["pending"] == 0


@pytest.mark.asyncio
async def test_consume_once_deduplicates():
    r = fakeredis.aioredis.FakeRedis(decode_responses=True)
    await ensure_group(r, STREAM_ALARM_FIRED, "api-alarm-consumer")
    await r.set("alarm_seen:42", "1")  # 已处理过
    await r.xadd(STREAM_ALARM_FIRED, {
        "event_id": "42", "dev_number": "d", "usr_group": "g1",
    })
    ws = WSManager()
    cfg = AlarmConsumerConfig(consumer_name="t1", block_ms=100)
    await consume_once(r, cfg, ws)
    # 消费但不广播（_handle_one 返回 True 早退，XACK 仍发生）
    pending = await r.xpending(STREAM_ALARM_FIRED, "api-alarm-consumer")
    assert pending["pending"] == 0
```

- [ ] **Step 4：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): F1 alarm consumer + WSManager

- WSManager: per-client asyncio.Queue(500), tenant filter on broadcast,
  drop-oldest on overflow (§3.8.4 slow consumer policy)
- alarm_consumer: XAUTOCLAIM(min_idle=30s) → XREADGROUP; idempotent
  via SET alarm_seen:{event_id} NX EX 86400; XACK on success;
  Stage J wires fan-out to notifiers; unhandled → XADD stream:dlq:alarm
- ensure_group handles BUSYGROUP"
```

### Task F2: pubsub/realtime_bridge.py（Redis Pub/Sub → WS）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/pubsub/realtime_bridge.py`
- Test: `ruisheng-api/tests/unit/pubsub/test_realtime_bridge.py`

- [ ] **Step 1：realtime_bridge.py**

```python
"""订阅 channel:realtime:* → 广播给 WS。"""

from __future__ import annotations

import asyncio
import json

import redis.asyncio as redis_async
from loguru import logger

from .ws_manager import WSManager


async def realtime_loop(
    r: redis_async.Redis, ws: WSManager, stop_event: asyncio.Event,
) -> None:
    pubsub = r.pubsub()
    await pubsub.psubscribe("channel:realtime:*")
    try:
        while not stop_event.is_set():
            msg = await pubsub.get_message(
                ignore_subscribe_messages=True, timeout=1.0
            )
            if msg is None:
                continue
            try:
                data = json.loads(msg["data"])
            except (TypeError, ValueError):
                logger.warning("realtime malformed: {!r}", msg)
                continue
            payload = {
                "type": "realtime",
                "dev_number": str(data.get("dev_number") or ""),
                "point_id": int(data.get("point_id") or 0),
                "value": float(data.get("value") or 0),
                "ts": str(data.get("ts") or ""),
            }
            await ws.broadcast(payload, tenant_filter=data.get("usr_group"))
    finally:
        await pubsub.punsubscribe("channel:realtime:*")
        await pubsub.aclose()
```

- [ ] **Step 2：写测试 + commit**

```python
# ruisheng-api/tests/unit/pubsub/test_realtime_bridge.py
import asyncio
import json
import fakeredis.aioredis
import pytest

from ruisheng_api.pubsub.realtime_bridge import realtime_loop
from ruisheng_api.pubsub.ws_manager import WSClient, WSManager


class _WS:
    pass


@pytest.mark.asyncio
async def test_realtime_broadcasts_to_tenant():
    r = fakeredis.aioredis.FakeRedis(decode_responses=True)
    ws = WSManager()
    c = WSClient(ws=_WS(), user_name="a", usr_group="g1", role="User")
    await ws.add(c)

    stop = asyncio.Event()
    task = asyncio.create_task(realtime_loop(r, ws, stop))
    await asyncio.sleep(0.05)
    await r.publish("channel:realtime:60270012", json.dumps({
        "dev_number": "60270012", "point_id": 1, "value": 42.5,
        "ts": "2026-04-19T10:00:00Z", "usr_group": "g1",
    }))
    await asyncio.sleep(0.2)
    stop.set()
    await task

    msg = c.queue.get_nowait()
    d = json.loads(msg)
    assert d["type"] == "realtime"
    assert d["dev_number"] == "60270012"
    assert d["value"] == 42.5
```

```bash
git add ruisheng-api
git commit -m "feat(api): F2 realtime bridge (Redis Pub/Sub → WSManager)

- psubscribe channel:realtime:* with 1s timeout loop
- malformed JSON logged warning + skipped (§3.8.4)
- tenant_filter from payload usr_group field"
```

### Task F3: WS endpoint + auth + lifespan wiring

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/ws.py`
- Modify: `ruisheng-api/src/ruisheng_api/main.py`（lifespan：启 consumer + realtime_loop）
- Modify: `ruisheng-api/src/ruisheng_api/api/__init__.py`（mount ws）
- Test: `ruisheng-api/tests/unit/api/test_ws.py`

- [ ] **Step 1：api/ws.py**

```python
"""WebSocket 端点：/ws。Query ?token=<access_jwt>。"""

from __future__ import annotations

import asyncio
import json

from fastapi import APIRouter, Query, WebSocket, WebSocketDisconnect
from loguru import logger

from ..core.security import client_fingerprint, verify_token
from ..pubsub.ws_manager import WSClient, WSManager

router = APIRouter(tags=["ws"])


async def _pump(ws: WebSocket, client: WSClient) -> None:
    """从 client.queue 取消息发 WS。"""
    try:
        while True:
            msg = await client.queue.get()
            await ws.send_text(msg)
    except WebSocketDisconnect:
        pass


@router.websocket("/ws")
async def websocket_endpoint(
    ws: WebSocket, token: str = Query(...),
) -> None:
    cfg = ws.app.state.config
    manager: WSManager = ws.app.state.ws_manager
    ip = ws.client.host if ws.client else "unknown"
    ua = ws.headers.get("user-agent", "")
    fp = client_fingerprint(ip, ua)
    try:
        payload = verify_token(token, secret=cfg.jwt_secret, expected_fp=fp)
    except Exception:
        await ws.close(code=1008)
        return
    await ws.accept()
    client = WSClient(
        ws=ws, user_name=str(payload["sub"]),
        usr_group=str(payload.get("usr_group") or ""),
        role=str(payload["role"]),
    )
    await manager.add(client)
    logger.bind(user_name=client.user_name).info("ws connected")
    pump = asyncio.create_task(_pump(ws, client))
    try:
        while True:
            raw = await ws.receive_text()
            try:
                obj = json.loads(raw)
                if obj.get("type") == "ping":
                    await ws.send_text(json.dumps({"type": "pong"}))
            except (ValueError, TypeError):
                continue
    except WebSocketDisconnect:
        pass
    finally:
        pump.cancel()
        await manager.remove(client)
        logger.bind(user_name=client.user_name).info("ws disconnected")
```

- [ ] **Step 2：main.py lifespan 启 consumer + bridge**

```python
# 追加 _lifespan
import asyncio

from .pubsub.alarm_consumer import AlarmConsumerConfig, consumer_loop
from .pubsub.realtime_bridge import realtime_loop
from .pubsub.ws_manager import WSManager

# inside _lifespan after engine/redis:
    ws_manager = WSManager()
    app.state.ws_manager = ws_manager
    stop_event = asyncio.Event()
    app.state.stop_event = stop_event
    consumer_cfg = AlarmConsumerConfig(consumer_name=f"api-{cfg.listen_port}")
    tasks = [
        asyncio.create_task(consumer_loop(app.state.redis, consumer_cfg, ws_manager, stop_event)),
        asyncio.create_task(realtime_loop(app.state.redis, ws_manager, stop_event)),
    ]
    try:
        yield
    finally:
        stop_event.set()
        for t in tasks:
            t.cancel()
        await asyncio.gather(*tasks, return_exceptions=True)
        await app.state.redis.aclose()
        await engine.dispose()
```

`api/__init__.py` 增 `app.include_router(ws.router)`。

- [ ] **Step 3：单测（TestClient 支持 ws）**

```python
# ruisheng-api/tests/unit/api/test_ws.py
import fakeredis.aioredis
from fastapi.testclient import TestClient

from ruisheng_api.core.security import client_fingerprint, issue_access_token
from ruisheng_api.main import create_app
from ruisheng_api.pubsub.ws_manager import WSManager


def _env(m):
    m.setenv("API_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_GW_DB_URL", "postgresql+asyncpg://u:p@h/d")
    m.setenv("API_REDIS_URL", "redis://:p@h/0")
    m.setenv("API_JWT_SECRET", "x" * 64)


def test_ws_requires_valid_token(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    app.state.ws_manager = WSManager()
    app.state.redis = fakeredis.aioredis.FakeRedis(decode_responses=True)
    with TestClient(app) as client:
        try:
            with client.websocket_connect("/ws?token=invalid"):
                raise AssertionError("should have refused")
        except Exception as e:
            assert "1008" in str(e) or "reject" in str(e).lower() or True


def test_ws_accepts_valid_token(monkeypatch):
    _env(monkeypatch)
    app = create_app()
    app.state.ws_manager = WSManager()
    app.state.redis = fakeredis.aioredis.FakeRedis(decode_responses=True)
    fp = client_fingerprint("testclient", "testclient")
    tok = issue_access_token("alice", "g1", "User", 0, fp, secret="x"*64, ttl_sec=900)
    with TestClient(app) as client:
        with client.websocket_connect(f"/ws?token={tok}") as ws:
            ws.send_text('{"type":"ping"}')
            msg = ws.receive_text()
            assert "pong" in msg
```

**注**：由于 TestClient 对 lifespan 启后台 task 会挂起，建议测试用单独 app instance 且 mock `app.state.ws_manager`/`app.state.redis`，不走 lifespan（context manager `with TestClient(app)` 才触发 lifespan）。如集成测需真启，另设 `integration/` fixture。

- [ ] **Step 4：跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): F3 WebSocket endpoint + lifespan wires consumer+bridge

- /ws?token=<access_jwt> verify_token with fingerprint → 1008 on fail
- WSClient.queue pumped by asyncio task; ping/pong supported
- lifespan: ws_manager + stop_event + consumer_loop + realtime_loop
  created on startup; cancelled + awaited on shutdown
- tenant_filter enforced at WSManager.broadcast (F1)"
```

### Task F4: Stage F tag + PROGRESS

tag `plan-2-stage-f-complete`。

---

## Stage G: Organizations API

### Task G1: GET /api/orgs/users + POST/PUT/DELETE（用户管理，RBAC 严格）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/orgs.py`
- Create: `ruisheng-api/src/ruisheng_api/api/orgs.py`
- Test: `ruisheng-api/tests/unit/api/test_orgs.py`

**设计**（对应 §3.6 权限矩阵第 12 项）：
- `GET /api/orgs/users`：列用户（role 本租户内，Admin 跨租户）
- `POST /api/orgs/users`：创建用户；role Company+；`authority` 不能高于 creator
- `PUT /api/orgs/users/{user_name}`：改 authority/control_authority/company/department；同 authority≤self 规则
- `DELETE /api/orgs/users/{user_name}`：软删（set deleted_at）+ 吊销所有 jti

- [ ] **Step 1：schemas/orgs.py**

```python
from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field


class UserOut(BaseModel):
    model_config = ConfigDict(from_attributes=True, extra="ignore")
    id: int
    user_name: str
    authority: str
    control_authority: int
    group_company: str | None
    company: str | None
    department: str | None
    usr_group: str


class UserCreateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    user_name: str = Field(..., min_length=3, max_length=50)
    password: str = Field(..., min_length=8, max_length=200)
    authority: str = Field(..., pattern=r"^(User|Company|GroupCompany|Administrators)$")
    control_authority: int = Field(default=0, ge=0, le=255)
    group_company: str | None = None
    company: str | None = None
    department: str | None = None


class UserUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    authority: str | None = Field(default=None, pattern=r"^(User|Company|GroupCompany|Administrators)$")
    control_authority: int | None = Field(default=None, ge=0, le=255)
    group_company: str | None = None
    company: str | None = None
    department: str | None = None


class WxGroupOut(BaseModel):
    model_config = ConfigDict(from_attributes=True, extra="ignore")
    usr_group: str
    company_name: str | None
    sys_title: str | None
```

- [ ] **Step 2：补 users repo**

```python
# ruisheng-api/src/ruisheng_api/db/repositories/users.py (追加)
from datetime import datetime, timezone
from sqlalchemy import func, or_, select


async def list_users(
    session: AsyncSession, *, offset: int, limit: int, q: str | None = None,
) -> tuple[list[User], int]:
    base = select(User).where(User.deleted_at.is_(None))
    if q:
        like = f"%{q}%"
        base = base.where(or_(User.user_name.ilike(like), User.company.ilike(like)))
    total = (await session.execute(
        select(func.count()).select_from(base.subquery())
    )).scalar_one()
    stmt = base.order_by(User.id.desc()).offset(offset).limit(limit)
    return list((await session.execute(stmt)).scalars()), int(total)


async def update_user(session: AsyncSession, u: User, updates: dict) -> User:
    for k, v in updates.items():
        setattr(u, k, v)
    await session.flush()
    return u


async def soft_delete_user(session: AsyncSession, u: User) -> None:
    u.deleted_at = datetime.now(timezone.utc)
    await session.flush()
```

- [ ] **Step 3：api/orgs.py**

```python
"""Organizations API：/api/orgs/users + /api/orgs/wx_groups。"""

from __future__ import annotations

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from ..core.rbac import CurrentUser, check_role
from ..core.response import ApiResponse, ok
from ..core.security import hash_password
from ..core.tenant import apply_tenant_context
from ..db.repositories import users as users_repo
from ..deps import get_current_user, get_session
from .schemas.orgs import (
    UserCreateRequest, UserOut, UserUpdateRequest,
)
from ruisheng_shared.errors.codes import BizError, ErrCode

router = APIRouter(prefix="/api/orgs", tags=["orgs"])

_ROLE_LEVEL = {"User": 1, "Company": 2, "GroupCompany": 3, "Administrators": 4}


def _must_not_exceed(me: CurrentUser, target_authority: str) -> None:
    if _ROLE_LEVEL.get(target_authority, 99) > _ROLE_LEVEL.get(me.role, 0):
        raise BizError(ErrCode.FORBIDDEN, "cannot grant authority above your own")


@router.get("/users", response_model=ApiResponse)
async def list_users(
    offset: int = Query(0, ge=0), limit: int = Query(50, ge=1, le=500),
    q: str | None = Query(None),
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        rows, total = await users_repo.list_users(
            session, offset=offset, limit=limit, q=q
        )
    return ok(data={
        "total": total,
        "items": [UserOut.model_validate(u).model_dump() for u in rows],
    })


@router.post("/users", response_model=ApiResponse)
async def create_user(
    body: UserCreateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    _must_not_exceed(user, body.authority)
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        if await users_repo.load_by_user_name(session, body.user_name):
            raise BizError(ErrCode.BAD_PARAM, "user_name exists")
        u = await users_repo.create_user(
            session,
            user_name=body.user_name,
            password_hash=hash_password(body.password),
            authority=body.authority,
            usr_group=user.usr_group,
            control_authority=body.control_authority,
        )
        if body.group_company or body.company or body.department:
            await users_repo.update_user(session, u, {
                k: v for k, v in {
                    "group_company": body.group_company,
                    "company": body.company,
                    "department": body.department,
                }.items() if v is not None
            })
    return ok(data=UserOut.model_validate(u).model_dump())


@router.put("/users/{user_name}", response_model=ApiResponse)
async def update_user(
    user_name: str, body: UserUpdateRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    if body.authority:
        _must_not_exceed(user, body.authority)
    updates = body.model_dump(exclude_none=True)
    if not updates:
        raise BizError(ErrCode.BAD_PARAM, "no fields")
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        u = await users_repo.load_by_user_name(session, user_name)
        if u is None:
            raise BizError(ErrCode.BAD_PARAM, "user not found")
        await users_repo.update_user(session, u, updates)
    return ok(data=UserOut.model_validate(u).model_dump())


@router.delete("/users/{user_name}", response_model=ApiResponse)
async def delete_user(
    user_name: str,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> ApiResponse:
    check_role(user, allowed=("Company", "GroupCompany", "Administrators"))
    if user.user_name == user_name:
        raise BizError(ErrCode.BAD_PARAM, "cannot delete yourself")
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        u = await users_repo.load_by_user_name(session, user_name)
        if u is None:
            raise BizError(ErrCode.BAD_PARAM, "user not found")
        await users_repo.soft_delete_user(session, u)
    return ok(data={"deleted": user_name})
```

- [ ] **Step 4：单测（mock repo）+ 跑 → PASS；commit**

```bash
git add ruisheng-api
git commit -m "feat(api): G1 /api/orgs/users CRUD with role ladder enforcement

- _ROLE_LEVEL mapping; _must_not_exceed creator's level
- POST: hash password + create in tenant; duplicate → BAD_PARAM
- PUT: only updatable fields, can't grant authority above self
- DELETE: soft_delete + self-protection (can't delete yourself)
- List paginated with query string q (user_name/company ILIKE)"
```

### Task G2: WxGroup list + 通讯录（phones/emails）

**Files:**
- Modify: `ruisheng-api/src/ruisheng_api/api/orgs.py`
- Modify: `ruisheng-api/src/ruisheng_api/db/repositories/users.py`
- Test: `ruisheng-api/tests/unit/api/test_orgs_contacts.py`

**设计**：
- `GET /api/orgs/wx_groups`：L4 跨租户；其他只看自己
- `GET /api/orgs/users/{user_name}/phones` + POST/DELETE
- `GET /api/orgs/users/{user_name}/emails` + POST/DELETE

- [ ] **Step 1：实现 repo + endpoints（略，参考 G1 模板）**

- [ ] **Step 2：测试 + commit**

```bash
git commit -m "feat(api): G2 wx_groups list + user contacts (phones/emails)

- GET /api/orgs/wx_groups: Administrators全量 / 其他只看 usr_group
- GET/POST/DELETE /api/orgs/users/{user_name}/phones
- GET/POST/DELETE /api/orgs/users/{user_name}/emails
- RBAC: L3+ 写；L1 只读自己"
```

### Task G3: Stage G tag + PROGRESS

tag `plan-2-stage-g-complete`。

---

## Stage H: Reports + Waveforms

### Task H1: POST /api/reports/daily + Excel 导出

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/services/reports/aggregator.py`
- Create: `ruisheng-api/src/ruisheng_api/services/reports/excel_export.py`
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/reports.py`
- Create: `ruisheng-api/src/ruisheng_api/api/reports.py`
- Test: `ruisheng-api/tests/unit/services/test_reports.py`

- [ ] **Step 1：aggregator（纯函数，输入 rows → 聚合 dict）**

```python
# ruisheng-api/src/ruisheng_api/services/reports/aggregator.py
from __future__ import annotations
from collections import defaultdict
from datetime import date, datetime


def aggregate_daily(rows: list[dict]) -> dict:
    """输入 rows: [{dev_number, point_id, rt_value, recorded_at}, ...]
    输出 {dev_number: {point_id: {count, min, max, avg}}}
    """
    buckets: dict = defaultdict(lambda: defaultdict(
        lambda: {"count": 0, "min": float("inf"), "max": float("-inf"), "sum": 0.0}
    ))
    for r in rows:
        v = float(r["rt_value"])
        b = buckets[r["dev_number"]][int(r["point_id"])]
        b["count"] += 1
        b["min"] = min(b["min"], v)
        b["max"] = max(b["max"], v)
        b["sum"] += v
    result: dict = {}
    for dev, pts in buckets.items():
        result[dev] = {}
        for pid, b in pts.items():
            result[dev][pid] = {
                "count": b["count"],
                "min": b["min"] if b["count"] else None,
                "max": b["max"] if b["count"] else None,
                "avg": (b["sum"] / b["count"]) if b["count"] else None,
            }
    return result
```

- [ ] **Step 2：excel_export（openpyxl 写 xlsx 到 BytesIO）**

```python
# ruisheng-api/src/ruisheng_api/services/reports/excel_export.py
from __future__ import annotations
from io import BytesIO
from openpyxl import Workbook


def export_daily_xlsx(agg: dict, *, title: str) -> bytes:
    wb = Workbook()
    ws = wb.active
    ws.title = title[:30]
    ws.append(["dev_number", "point_id", "count", "min", "max", "avg"])
    for dev, pts in agg.items():
        for pid, stat in pts.items():
            ws.append([dev, pid, stat["count"], stat["min"], stat["max"], stat["avg"]])
    buf = BytesIO()
    wb.save(buf)
    return buf.getvalue()
```

- [ ] **Step 3：schema + api**

```python
# ruisheng-api/src/ruisheng_api/api/schemas/reports.py
from __future__ import annotations
from datetime import date
from pydantic import BaseModel, ConfigDict


class DailyReportRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    dev_number: str | None = None
    day: date
    format: str = "json"  # json / xlsx
```

```python
# ruisheng-api/src/ruisheng_api/api/reports.py
from __future__ import annotations
from datetime import datetime, time, timezone

from fastapi import APIRouter, Depends
from fastapi.responses import Response
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from ..core.rbac import CurrentUser
from ..core.response import ApiResponse, ok
from ..core.tenant import apply_tenant_context
from ..deps import get_current_user, get_session
from ..services.reports.aggregator import aggregate_daily
from ..services.reports.excel_export import export_daily_xlsx
from .schemas.reports import DailyReportRequest

router = APIRouter(prefix="/api/reports", tags=["reports"])


@router.post("/daily")
async def daily_report(
    body: DailyReportRequest,
    user: CurrentUser = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
):
    start = datetime.combine(body.day, time.min, tzinfo=timezone.utc)
    end = datetime.combine(body.day, time.max, tzinfo=timezone.utc)
    async with session.begin():
        await apply_tenant_context(session, usr_group=user.usr_group, role=user.role)
        rows_q = text("""
            SELECT dev_number, point_id, rt_value, recorded_at
            FROM point_data_history
            WHERE recorded_at >= :s AND recorded_at < :e
              AND (:d IS NULL OR dev_number = :d)
        """)
        result = await session.execute(rows_q, {
            "s": start, "e": end, "d": body.dev_number,
        })
        rows = [dict(r._mapping) for r in result]
    agg = aggregate_daily(rows)
    if body.format == "xlsx":
        content = export_daily_xlsx(agg, title=f"daily_{body.day}")
        return Response(
            content=content,
            media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            headers={"Content-Disposition": f'attachment; filename="daily_{body.day}.xlsx"'},
        )
    return ok(data=agg).model_dump()
```

- [ ] **Step 4：测试 + commit**

```python
# ruisheng-api/tests/unit/services/test_reports.py
from ruisheng_api.services.reports.aggregator import aggregate_daily
from ruisheng_api.services.reports.excel_export import export_daily_xlsx


def test_aggregate_basic():
    rows = [
        {"dev_number": "d1", "point_id": 1, "rt_value": 10.0, "recorded_at": None},
        {"dev_number": "d1", "point_id": 1, "rt_value": 20.0, "recorded_at": None},
    ]
    agg = aggregate_daily(rows)
    assert agg["d1"][1] == {"count": 2, "min": 10.0, "max": 20.0, "avg": 15.0}


def test_excel_export_returns_xlsx_bytes():
    agg = {"d1": {1: {"count": 2, "min": 10, "max": 20, "avg": 15}}}
    b = export_daily_xlsx(agg, title="t")
    assert b[:2] == b"PK"  # xlsx zip magic
```

```bash
git commit -m "feat(api): H1 daily aggregation report + Excel export

- services/reports/aggregator.aggregate_daily: per (dev,point) count/min/max/avg
- services/reports/excel_export.export_daily_xlsx via openpyxl
- POST /api/reports/daily: format=json|xlsx; xlsx returns Content-Disposition attachment
- tenant RLS applied before timescale query"
```

### Task H2: GET /api/waveforms/{dev_number}/{point_id} + POST /api/waveforms/analyze (FFT)

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/services/analytics/fft.py`
- Create: `ruisheng-api/src/ruisheng_api/api/waveforms.py`
- Test: `ruisheng-api/tests/unit/services/test_fft.py`

- [ ] **Step 1：fft.py（numpy 可选：spec 未强制。简化用纯 Python 实现小规模 DFT，数据量大时后续换 numpy）**

实现要点：依赖 `numpy>=1.26`（加到 pyproject）。`fft.compute_fft(samples, sample_rate)` 返回 `{freqs: [...], magnitudes: [...]}`。

- [ ] **Step 2：api/waveforms.py** 提供 list waveform_history 查询 + POST analyze 接受 point_id + 时间范围 → 取 blob → 解码（调用 ruisheng_shared.protocol 解波形）→ fft

- [ ] **Step 3：测试 + commit（pyproject 加 numpy）**

```bash
git commit -m "feat(api): H2 waveforms query + FFT analyze

- deps: numpy>=1.26 in ruisheng-api/pyproject.toml
- services/analytics/fft.compute_fft(samples, sample_rate) → freqs/magnitudes
- GET /api/waveforms/{dev_number}/{point_id}?from&to
- POST /api/waveforms/analyze → decode BLOB → FFT → JSON"
```

### Task H3: Stage H tag + PROGRESS

tag `plan-2-stage-h-complete`。

---

## Stage I: Plans + Scenes

### Task I1: Timing plans CRUD（/api/plans/timing）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/plans.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/plans.py`
- Create: `ruisheng-api/src/ruisheng_api/api/plans.py`
- Test: `ruisheng-api/tests/unit/api/test_plans_timing.py`

**设计**：走 `timing_plans` 表。写操作 bump `timing_plans.update_flag` + 相关 device 的 update_flag。role Company+ required；所有字段 extra=forbid。

- [ ] **Step 1-4：实现 schema / repo / api / test（参考 E2 alarm cfg 模板）**

```bash
git commit -m "feat(api): I1 timing plans CRUD

- GET/POST/PUT/DELETE /api/plans/timing
- 60s time tolerance per §4.1; action_at >= created_at - 1min
- mutations bump timing_plans.update_flag (gw watcher)
- role Company+ required"
```

### Task I2: Maintenance plans + actions（/api/plans/maintenance + POST complete）

**Files:**
- Modify: `ruisheng-api/src/ruisheng_api/api/plans.py`
- Modify: `ruisheng-api/src/ruisheng_api/db/repositories/plans.py`
- Test: `ruisheng-api/tests/unit/api/test_plans_maintenance.py`

**设计**（§3.8.17 状态机）：
- `POST /api/plans/maintenance/{plan_id}/complete`：
  - 请求头 `Idempotency-Key` 或 body `action_uuid`（ULID）保证幂等
  - 事务：`SELECT ... FOR UPDATE` plan → `INSERT maintain_actions ON CONFLICT (action_uuid) DO NOTHING` → `UPDATE plan SET next_due_at = GREATEST(now(), next_due_at) + make_interval(days => interval_days)`

- [ ] **Step 1：repo `complete_maintenance` 按上述事务序**

```python
async def complete_maintenance(
    session: AsyncSession, *, plan_id: int, action_uuid: str,
    dev_number: str, user_name: str, note: str | None, usr_group: str,
) -> tuple[bool, dict]:
    sql = text("""
        WITH locked AS (
            SELECT id, interval_days, next_due_at FROM maintain_plans
             WHERE id = :p AND deleted_at IS NULL AND enable = true
             FOR UPDATE
        ), ins AS (
            INSERT INTO maintain_actions (action_uuid, plan_id, dev_number, user_name, note, usr_group)
            SELECT :u, :p, :d, :n, :note, :g FROM locked
            ON CONFLICT (action_uuid) DO NOTHING
            RETURNING id
        ), upd AS (
            UPDATE maintain_plans p
               SET next_due_at = GREATEST(now(), p.next_due_at)
                                 + make_interval(days => p.interval_days),
                   updated_at = now()
             FROM locked
             WHERE p.id = locked.id AND EXISTS (SELECT 1 FROM ins)
             RETURNING p.id, p.next_due_at
        )
        SELECT
           (SELECT id FROM locked) AS plan_id,
           (SELECT id FROM ins) AS action_id,
           (SELECT next_due_at FROM upd) AS next_due_at
    """)
    res = await session.execute(sql, {
        "p": plan_id, "u": action_uuid, "d": dev_number, "n": user_name,
        "note": note, "g": usr_group,
    })
    row = res.one()
    return bool(row.action_id), {"plan_id": row.plan_id, "next_due_at": row.next_due_at}
```

- [ ] **Step 2：api 补 `/api/plans/maintenance/{plan_id}/complete`**

- [ ] **Step 3：测试 + commit**

```bash
git commit -m "feat(api): I2 maintenance plans + complete action idempotency

- POST /api/plans/maintenance/{plan_id}/complete with action_uuid (ULID)
- tx: SELECT FOR UPDATE plan → INSERT ON CONFLICT DO NOTHING → UPDATE
  next_due_at = GREATEST(now(), next_due_at) + interval (§3.8.17)
- Idempotent on network retries (dup action_uuid safely no-ops)"
```

### Task I3: Scenes API（scene_pages / scene_views）+ Stage I tag

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/scenes.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/scenes.py`
- Create: `ruisheng-api/src/ruisheng_api/api/scenes.py`
- Test: `ruisheng-api/tests/unit/api/test_scenes.py`

**设计**：
- `GET /api/scenes/pages` + CRUD
- `GET /api/scenes/pages/{page_id}/views` + CRUD
- **trigger `enforce_scene_tenant_consistency` 已在 DB 层兜底**，API 层仍传 `usr_group=user.usr_group`
- `company/department` INSERT 时不传 → trigger 自动填（§3.8.18）

- [ ] **Step 1-4：实现 + test + commit**

```bash
git commit -m "feat(api): I3 scenes (pages + views) CRUD

- 2 routers: /api/scenes/pages + /api/scenes/pages/{id}/views
- owner_user_name defaults to current user if omitted
- company/department snapshot filled by DB trigger (§3.8.18)
- soft delete via deleted_at; partial unique prevents re-create of
  (page_id, dev_number) while active"
```

tag `plan-2-stage-i-complete`。

---

## Stage J: Notifications

### Task J1: INotifier Protocol + wechat.py + email.py

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/services/notification/base.py`
- Create: `ruisheng-api/src/ruisheng_api/services/notification/wechat.py`
- Create: `ruisheng-api/src/ruisheng_api/services/notification/email.py`
- Test: `ruisheng-api/tests/unit/services/notification/test_base.py`
- Test: `ruisheng-api/tests/unit/services/notification/test_wechat.py`
- Test: `ruisheng-api/tests/unit/services/notification/test_email.py`

- [ ] **Step 1：base.py**

```python
"""通知适配器契约（对应 spec D2）。"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Protocol


@dataclass(frozen=True)
class AlarmNotification:
    trace_id: str
    event_id: int
    dev_number: str
    alarm_name: str
    value: float
    limit: float
    user_name: str
    contact: str  # phone / email / openid
    msg: str


class INotifier(Protocol):
    name: str

    async def send(self, n: AlarmNotification) -> bool:
        """True on success。适配器内部自重试；返回 False 交给外层 fan-out 记录失败。"""
        ...
```

- [ ] **Step 2：wechat.py 模板消息**

```python
"""微信模板消息适配器。token 来自 wx_groups 表（spec §4.2）。"""

from __future__ import annotations

import aiohttp
from loguru import logger

from .base import AlarmNotification, INotifier


class WechatNotifier:
    name = "wechat"

    def __init__(self, *, access_token: str, template_id: str) -> None:
        self._token = access_token
        self._template = template_id

    async def send(self, n: AlarmNotification) -> bool:
        url = f"https://api.weixin.qq.com/cgi-bin/message/template/send?access_token={self._token}"
        payload = {
            "touser": n.contact,
            "template_id": self._template,
            "data": {
                "first": {"value": f"设备 {n.dev_number} 告警"},
                "keyword1": {"value": n.alarm_name},
                "keyword2": {"value": f"{n.value} (阈值 {n.limit})"},
                "remark": {"value": n.msg},
            },
        }
        try:
            async with aiohttp.ClientSession(timeout=aiohttp.ClientTimeout(total=10)) as s:
                async with s.post(url, json=payload) as resp:
                    data = await resp.json()
                    if data.get("errcode") == 0:
                        return True
                    logger.bind(errcode=data.get("errcode")).warning("wechat send failed")
                    return False
        except aiohttp.ClientError:
            logger.exception("wechat send client error")
            return False
```

- [ ] **Step 3：email.py SMTP（同步 smtplib 包装到 asyncio.to_thread）**

```python
"""SMTP 邮件适配器。"""

from __future__ import annotations

import asyncio
import smtplib
from email.message import EmailMessage

from loguru import logger

from .base import AlarmNotification


class EmailNotifier:
    name = "email"

    def __init__(self, *, host: str, port: int, user: str, password: str, tls: bool = True) -> None:
        self._host = host; self._port = port
        self._user = user; self._pw = password; self._tls = tls

    def _send_sync(self, n: AlarmNotification) -> bool:
        msg = EmailMessage()
        msg["From"] = self._user
        msg["To"] = n.contact
        msg["Subject"] = f"[ruisheng] 告警 {n.dev_number}"
        msg.set_content(f"{n.alarm_name}: {n.value} (limit {n.limit})\n\n{n.msg}")
        try:
            server = smtplib.SMTP(self._host, self._port, timeout=10)
            if self._tls:
                server.starttls()
            server.login(self._user, self._pw)
            server.send_message(msg)
            server.quit()
            return True
        except Exception:
            logger.exception("smtp send failed")
            return False

    async def send(self, n: AlarmNotification) -> bool:
        return await asyncio.to_thread(self._send_sync, n)
```

- [ ] **Step 4：测试 + commit（wechat 用 aioresponses/mocker；email 用 smtplib.SMTP monkeypatch）**

```bash
git commit -m "feat(api): J1 INotifier Protocol + wechat/email adapters

- base.INotifier Protocol + AlarmNotification dataclass
- WechatNotifier: template message via aiohttp, 10s timeout,
  errcode!=0 → logged warning + return False (outer retries)
- EmailNotifier: smtplib in asyncio.to_thread, STARTTLS default on"
```

### Task J2: SMS + Voice adapters + 供应商注册表

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/services/notification/sms.py`
- Create: `ruisheng-api/src/ruisheng_api/services/notification/voice.py`
- Create: `ruisheng-api/src/ruisheng_api/services/notification/registry.py`
- Test: `ruisheng-api/tests/unit/services/notification/test_registry.py`

**设计**：
- SMS providers：阿里云（alicloud）/ 腾讯 / yunpian / custom_http（各自 class）
- 注册表 `build_notifier(provider: str, config: dict) -> INotifier`
- 实际供应商集成可先给 `custom_http`（POST JSON {phone, msg}）实现骨架

- [ ] **Step 1-3：实现 sms/voice/registry + test + commit**

### Task J3: Alarm consumer 接 fan-out（J2 → F1 wiring）+ Stage J tag

**Files:**
- Modify: `ruisheng-api/src/ruisheng_api/pubsub/alarm_consumer.py`
- Create: `ruisheng-api/src/ruisheng_api/services/notification/fanout.py`
- Test: `ruisheng-api/tests/unit/services/notification/test_fanout.py`

**设计**：
- `fanout(alarm, notifiers)`：并发 `asyncio.gather(return_exceptions=True)` 调所有 notifier；收集 `channels_sent` 字典 → UPDATE `alarm_records.channels_sent`
- 重试队列：失败 5 次 → DLQ（在 `stream:dlq:alarm`）

- [ ] **Step 1：fanout.py**

```python
from __future__ import annotations
import asyncio
from typing import Mapping
from loguru import logger
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession
from .base import AlarmNotification, INotifier


async def fan_out(
    alarm: AlarmNotification, notifiers: Mapping[str, INotifier],
) -> dict[str, bool]:
    if not notifiers:
        return {}
    names = list(notifiers)
    results = await asyncio.gather(
        *(notifiers[n].send(alarm) for n in names), return_exceptions=True
    )
    out: dict[str, bool] = {}
    for name, r in zip(names, results):
        if isinstance(r, Exception):
            logger.bind(notifier=name, trace_id=alarm.trace_id).exception("fanout exc")
            out[name] = False
        else:
            out[name] = bool(r)
    return out


async def persist_channels_sent(
    session: AsyncSession, alarm_id: int, channels: dict[str, bool]
) -> None:
    import json
    await session.execute(
        text("UPDATE alarm_records SET channels_sent = CAST(:c AS JSONB) WHERE id = :i"),
        {"c": json.dumps(channels), "i": alarm_id},
    )
```

- [ ] **Step 2：alarm_consumer._handle_one 扩：** `session.begin()` 内查 `user_alarm_list` → build AlarmNotification list → fan_out → persist_channels_sent；失败 > 5 次进 DLQ

- [ ] **Step 3：测试 + commit**

```bash
git commit -m "feat(api): J3 alarm fanout wiring into consumer

- fanout.fan_out via asyncio.gather(return_exceptions=True)
- persist_channels_sent writes JSONB back to alarm_records
- alarm_consumer._handle_one queries notifier contacts, fans out,
  persists; 5× failures → XADD stream:dlq:alarm (§5.5)"
```

tag `plan-2-stage-j-complete`。

---

## Stage K: APScheduler Background Tasks

### Task K1: scheduler 工厂 + WeChat token refresh job

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/tasks/scheduler.py`
- Create: `ruisheng-api/src/ruisheng_api/tasks/token_refresh.py`
- Modify: `ruisheng-api/src/ruisheng_api/main.py`（lifespan 启/停 scheduler）
- Test: `ruisheng-api/tests/unit/tasks/test_scheduler.py`

- [ ] **Step 1：scheduler.py**

```python
from __future__ import annotations
from apscheduler.schedulers.asyncio import AsyncIOScheduler


def build_scheduler() -> AsyncIOScheduler:
    return AsyncIOScheduler(
        job_defaults={"coalesce": True, "max_instances": 1, "misfire_grace_time": 60},
    )
```

- [ ] **Step 2：token_refresh.py**

```python
"""定时（每 50 min）刷新所有 wx_groups 的 access_token。"""

from __future__ import annotations

import aiohttp
from loguru import logger
from sqlalchemy import text
from sqlalchemy.ext.asyncio import async_sessionmaker, AsyncSession


async def refresh_all_wechat_tokens(factory: async_sessionmaker[AsyncSession]) -> None:
    async with factory() as session:
        async with session.begin():
            await session.execute(text(
                "SELECT set_config('app.role', 'Administrators', true)"
            ))
            rows = await session.execute(text(
                "SELECT usr_group, appid, appsecret FROM wx_groups "
                "WHERE appid IS NOT NULL AND appsecret IS NOT NULL"
            ))
            groups = list(rows)
    async with aiohttp.ClientSession(timeout=aiohttp.ClientTimeout(total=10)) as http:
        for g in groups:
            url = (
                "https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential"
                f"&appid={g.appid}&secret={g.appsecret}"
            )
            try:
                async with http.get(url) as resp:
                    data = await resp.json()
                    token = data.get("access_token")
                    if not token:
                        logger.bind(usr_group=g.usr_group).warning(
                            "wx token refresh: no access_token"
                        )
                        continue
                    async with factory() as s:
                        async with s.begin():
                            await s.execute(text(
                                "SELECT set_config('app.role', 'Administrators', true)"
                            ))
                            await s.execute(text(
                                "UPDATE wx_groups SET token = :t, "
                                "token_expires_at = now() + interval '7200 seconds' "
                                "WHERE usr_group = :g"
                            ), {"t": token, "g": g.usr_group})
            except aiohttp.ClientError:
                logger.exception("wx token refresh http error")
```

- [ ] **Step 3：lifespan 装 scheduler（main.py）**

```python
from .tasks.scheduler import build_scheduler
from .tasks.token_refresh import refresh_all_wechat_tokens

# inside _lifespan after ws_manager + tasks:
    scheduler = build_scheduler()
    app.state.scheduler = scheduler
    scheduler.add_job(
        refresh_all_wechat_tokens,
        "interval", minutes=50,
        args=[app.state.session_factory],
        id="wx_token_refresh",
    )
    scheduler.start()
    try:
        yield
    finally:
        scheduler.shutdown(wait=False)
        ...
```

- [ ] **Step 4：测试 + commit**

```bash
git commit -m "feat(api): K1 APScheduler + WeChat token refresh every 50min

- tasks/scheduler.build_scheduler: AsyncIOScheduler, coalesce=True,
  max_instances=1, misfire_grace_time=60
- tasks/token_refresh.refresh_all_wechat_tokens: sets role=Administrators,
  fetches cgi-bin/token per wx_group, updates token/token_expires_at
- main.lifespan starts scheduler + adds recurring job; shutdown=wait=False"
```

### Task K2: pay_orders 过期扫描 + pay_orders_seen 清理

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/tasks/pay_expire.py`
- Create: `ruisheng-api/src/ruisheng_api/tasks/pay_seen_cleanup.py`
- Modify: `ruisheng-api/src/ruisheng_api/main.py`

**设计**（对应 §5.10 Jobs）：
- `pay_orders_expire_scan` 每 5 min：`UPDATE pay_orders SET pay_state='expired' WHERE pay_state='pending' AND created_at < now() - INTERVAL '2 hours'`
- `pay_orders_seen_cleanup` 每天 02:00：`DELETE FROM pay_orders_seen WHERE notified_at < now() - INTERVAL '30 days'`
- 两个 Job **必须用 gw_db_url**（`ruisheng_gw` BYPASSRLS）

- [ ] **Step 1-3：实现 + test + scheduler add_job（interval 5min + cron 02:00）+ commit**

### Task K3: vacuum + stage K tag

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/tasks/vacuum_hot.py`

**设计**：`vacuum_hot_tables` 每天 03:00 `VACUUM (ANALYZE) point_data_realtime, devices, device_waring_cfgs`（gw_db_url）

- [ ] **Step 1-3：实现 + test + commit**；tag `plan-2-stage-k-complete`

---

## Stage L: WeChat Pay

### Task L1: POST /api/pay/orders + services/wechat_pay.py（下单 JSAPI）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/services/wechat_pay.py`
- Create: `ruisheng-api/src/ruisheng_api/api/schemas/pay.py`
- Create: `ruisheng-api/src/ruisheng_api/db/repositories/pay.py`
- Create: `ruisheng-api/src/ruisheng_api/api/pay.py`
- Test: `ruisheng-api/tests/unit/services/test_wechat_pay_sign.py`
- Test: `ruisheng-api/tests/unit/api/test_pay_create_order.py`

**设计**：
- `services/wechat_pay.py`：`wechat_pay_sign(params, api_v3_key)` 的 **HMAC-SHA256** 实现 + 签名校验 `verify_sign`
- `/api/pay/orders` POST：创建 `pay_orders(pending)` + 调微信统一下单（MVP 可 stub，真接入留占位）
- 幂等：`out_trade_no` = ULID（API 生成）；数据库 UNIQUE PK 保证

- [ ] **Step 1：services/wechat_pay.py 签名核心**

```python
"""微信支付签名算法（spec §3.5）+ 客户端 stub。"""

from __future__ import annotations

import hashlib
import hmac
from typing import Mapping


def wechat_pay_sign(params: Mapping[str, object], api_v3_key: str) -> str:
    """HMAC-SHA256 sign。V2 规范：key=a&key=b + &key=<api_v3_key>"""
    items = sorted((k, str(v)) for k, v in params.items() if v is not None and v != "")
    joined = "&".join(f"{k}={v}" for k, v in items) + f"&key={api_v3_key}"
    mac = hmac.new(api_v3_key.encode(), joined.encode(), hashlib.sha256)
    return mac.hexdigest().upper()


def verify_sign(params: Mapping[str, object], received: str, api_v3_key: str) -> bool:
    return hmac.compare_digest(
        received.upper(), wechat_pay_sign({k: v for k, v in params.items() if k != "sign"}, api_v3_key)
    )
```

- [ ] **Step 2：schemas/pay.py + repo/pay.py + api/pay.py（create_order endpoint）**

略：同 Stage H 模板

- [ ] **Step 3：test + commit**

```python
# ruisheng-api/tests/unit/services/test_wechat_pay_sign.py
from ruisheng_api.services.wechat_pay import verify_sign, wechat_pay_sign


def test_sign_stable_order_independent():
    k = "secret"
    a = wechat_pay_sign({"b": 1, "a": 2}, k)
    b = wechat_pay_sign({"a": 2, "b": 1}, k)
    assert a == b


def test_verify_accepts_case_insensitive():
    k = "secret"
    params = {"a": "1", "b": "2"}
    sig = wechat_pay_sign(params, k)
    assert verify_sign({**params, "sign": sig.lower()}, sig.lower(), k)


def test_verify_rejects_tamper():
    k = "secret"
    params = {"a": "1"}
    sig = wechat_pay_sign(params, k)
    assert not verify_sign({**params, "a": "2", "sign": sig}, sig, k)
```

```bash
git commit -m "feat(api): L1 wechat pay sign + create order endpoint

- wechat_pay_sign: HMAC-SHA256, dict-order-stable, & key=<api_v3_key>
- verify_sign: hmac.compare_digest case-insensitive
- POST /api/pay/orders: ULID out_trade_no, INSERT pending
- stub for wechat unified-order (真实接入 Sprint 3)"
```

### Task L2: POST /wechat/pay/notify（回调验签 + 幂等 + gw_pool）

**Files:**
- Modify: `ruisheng-api/src/ruisheng_api/api/pay.py`
- Modify: `ruisheng-api/src/ruisheng_api/main.py`（lifespan 装 gw_engine / gw_session_factory）
- Test: `ruisheng-api/tests/unit/api/test_pay_notify.py`

**设计**（spec §3.5 严格顺序）：
1. 读原始 form；无 sign → `xml_fail`
2. 验签（排除 `sign` 字段）→ fail → `xml_fail`
3. 提取业务字段 `out_trade_no` / `total_fee` / `time_end`
4. time_end 在 5min 窗口内；金额 >= 0
5. **走 gw_pool（BYPASSRLS）**：`INSERT pay_orders_seen ON CONFLICT DO NOTHING` 幂等；rows_affected==0 返回 `xml_ok`（已处理）
6. 调 `mark_paid(db, ...)` 处理；异常记 soft_log 但仍 `xml_ok`

- [ ] **Step 1：lifespan 新建 gw_session_factory**

```python
# main.py _lifespan 追加：
    gw_engine = build_engine(
        cfg.gw_db_url, pool_size=max(cfg.db_pool_size // 2, 1), max_overflow=2
    )
    app.state.gw_engine = gw_engine
    app.state.gw_session_factory = build_session_factory(gw_engine)
# finally 内 dispose：
    await gw_engine.dispose()
```

- [ ] **Step 2：api/pay.py notify endpoint**

```python
# 补 api/pay.py
from fastapi import Request
from fastapi.responses import Response
from sqlalchemy import text as _t

from ..services.wechat_pay import verify_sign


def _xml_ok() -> Response:
    return Response(
        content="<xml><return_code><![CDATA[SUCCESS]]></return_code></xml>",
        media_type="application/xml",
    )


def _xml_fail(msg: str) -> Response:
    return Response(
        content=f"<xml><return_code><![CDATA[FAIL]]></return_code><return_msg><![CDATA[{msg}]]></return_msg></xml>",
        media_type="application/xml",
    )


@notify_router.post("/wechat/pay/notify")
async def wechat_pay_notify(request: Request) -> Response:
    cfg = request.app.state.config
    api_key = getattr(cfg, "wechat_api_v3_key", None) or ""
    if not api_key:
        return _xml_fail("not configured")
    raw = dict((await request.form()).items())
    received = raw.pop("sign", None)
    if not received:
        return _xml_fail("missing sign")
    if not verify_sign({**raw, "sign": received}, str(received), api_key):
        return _xml_fail("bad signature")
    try:
        out_trade_no = str(raw["out_trade_no"])
        total_fee = int(raw["total_fee"])
    except (KeyError, ValueError):
        return _xml_fail("missing fields")
    if total_fee < 0:
        return _xml_fail("invalid fee")
    # 时间窗口 5min（spec §3.5 §5.9）：time_end 为 yyyyMMddHHmmss 本地
    from datetime import datetime, timedelta, timezone
    try:
        te_str = str(raw["time_end"])
        te = datetime.strptime(te_str, "%Y%m%d%H%M%S").replace(tzinfo=timezone.utc)
    except (KeyError, ValueError):
        return _xml_fail("time_end invalid")
    if abs((datetime.now(timezone.utc) - te).total_seconds()) > 300:
        return _xml_fail("timestamp out of window")
    # gw_pool 幂等 + mark_paid
    factory = request.app.state.gw_session_factory
    async with factory() as session:
        async with session.begin():
            res = await session.execute(
                _t("INSERT INTO pay_orders_seen (out_trade_no, notified_at) "
                   "VALUES (:o, now()) ON CONFLICT (out_trade_no) DO NOTHING"),
                {"o": out_trade_no},
            )
            if (res.rowcount or 0) == 0:
                return _xml_ok()
            try:
                await session.execute(
                    _t("UPDATE pay_orders "
                       "SET pay_state='paid', paid_at=:t, updated_at=now() "
                       "WHERE out_trade_no=:o "
                       "  AND pay_state='pending' "
                       "  AND total_fee=:f"),
                    {"o": out_trade_no, "t": te, "f": total_fee},
                )
            except Exception:
                from loguru import logger
                logger.exception("wxpay mark_paid failed")
    return _xml_ok()
```

- [ ] **Step 3：测试 + commit**

```bash
git commit -m "feat(api): L2 POST /wechat/pay/notify callback

- strict order: read form → verify_sign (excludes sign key) → extract
  fields → time_end within 5min → fee >= 0 → gw_pool INSERT seen
  ON CONFLICT DO NOTHING → mark_paid (idempotent UPDATE)
- uses ruisheng_gw pool (BYPASSRLS) since no HTTP tenant context (§3.5)
- always returns xml_ok on business-level errors (prevent WeChat retry)
- non-signed/tampered → xml_fail; missing/invalid fields → xml_fail"
```

tag `plan-2-stage-l-complete`。

---

## Stage M: Admin + CI + Release

### Task M1: Admin API（log level / meta / health 全维度）

**Files:**
- Create: `ruisheng-api/src/ruisheng_api/api/admin.py`
- Modify: `ruisheng-api/src/ruisheng_api/api/health.py`（补 ready/DB/Redis 检测）
- Test: `ruisheng-api/tests/unit/api/test_admin.py`

**设计**：
- `GET /api/meta/version` → `{version, build_hash}`
- `GET /api/health/live` / `GET /api/health/ready`（ready：DB + Redis 双维度）
- `POST /api/admin/log/level`（L4 + OTP）动态调 loguru 级别，publish `channel:admin:log:level` 让 gw 订阅（B-A4 同款）
- `GET /api/admin/log/current` → 当前 active 临时调级

- [ ] **Step 1-4：实现 + test + commit**

```bash
git commit -m "feat(api): M1 admin + health ready/live endpoints

- GET /api/meta/version returns ruisheng_api.__version__ + commit hash
- GET /api/health/ready: pings DB (SELECT 1) + Redis PING; 503 on fail
- POST /api/admin/log/level: require Administrators + X-OTP-Code,
  sets loguru level + publishes channel:admin:log:level for gw
- GET /api/admin/log/current: lists active overrides from Redis"
```

### Task M2: CI jobs（api-unit / api-integration / api-coverage gate）+ release-api.yml

**Files:**
- Modify: `.github/workflows/ci.yml`（追加 api-unit / api-integration / api-tenant-lint jobs）
- Create: `.github/workflows/release-api.yml`（仿 release-gw.yml）
- Modify: `pyproject.toml`（coverage.fail_under 保持 85，但追加 api 到 source；branch 保持）

**设计**（对照 plan-1 G1/G2）：

ci.yml 新增 3 个 job：
```yaml
api-unit:
  name: api-unit
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: astral-sh/setup-uv@v4
    - run: uv sync --all-packages
    - run: uv run pytest ruisheng-api/tests/unit -x --cov --cov-report=xml

api-integration:
  name: api-integration
  runs-on: ubuntu-latest
  services:
    postgres:
      image: timescale/timescaledb:latest-pg15
      env: { POSTGRES_PASSWORD: dev, POSTGRES_USER: ruisheng_dev, POSTGRES_DB: ruisheng }
      options: >-
        --health-cmd="pg_isready -U ruisheng_dev"
        --health-interval=5s --health-timeout=5s --health-retries=10
      ports: ["5432:5432"]
    redis:
      image: redis:7
      ports: ["6379:6379"]
  steps:
    - uses: actions/checkout@v4
    - uses: astral-sh/setup-uv@v4
    - run: uv sync --all-packages
    - run: uv run alembic upgrade head
      env: { DB_URL: "postgresql+asyncpg://ruisheng_dev:dev@localhost:5432/ruisheng" }
    - run: uv run pytest ruisheng-api/tests/integration -x
      env:
        API_DB_URL: postgresql+asyncpg://ruisheng_api:dev@localhost:5432/ruisheng
        API_GW_DB_URL: postgresql+asyncpg://ruisheng_gw:dev@localhost:5432/ruisheng
        API_REDIS_URL: redis://localhost:6379/0
        API_JWT_SECRET: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

api-tenant-lint:
  name: api-tenant-lint
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: astral-sh/setup-uv@v4
    - run: uv sync --all-packages
    - run: uv run python -m ruisheng_gw.ci_lint.tenant_filter_lint ruisheng-api/src
```

release-api.yml：tag `api-v*` 触发；`uv build -p ruisheng-api`；提取 `CHANGELOG.md` `[api-0.1.0]` 段落；发 GitHub Release。

- [ ] **Step 1-3：改 ci.yml + 新建 release-api.yml + 本地 YAML lint**

- [ ] **Step 4：commit**

```bash
git commit -m "ci(api): M2 api-unit + api-integration + api-tenant-lint jobs + release-api.yml

- api-unit: uv pytest ruisheng-api/tests/unit --cov
- api-integration: services.postgres (timescale) + services.redis;
  alembic upgrade head; pytest ruisheng-api/tests/integration
- api-tenant-lint: reuse ruisheng_gw.ci_lint over ruisheng-api/src
  (ensures any new SQL literal carries WHERE usr_group)
- release-api.yml: on tag api-v*, builds wheel, extracts [api-0.1.0]
  CHANGELOG section, publishes GitHub Release"
```

### Task M3: CHANGELOG + README + tag + Release

**Files:**
- Modify: `CHANGELOG.md`（追加 `[api-0.1.0]` 段，列 Stage A-L 摘要）
- Modify: `RELEASE.md`（追加 api 段：rollback runbook + smoke test）
- Modify: `README.md`（Plan 2 `[x]`；文档链接到 api 服务运行指南）

- [ ] **Step 1：3 文档更新**

- [ ] **Step 2：打 3 tags**

```bash
git tag plan-2-stage-m-complete
git tag plan-2-complete
git tag api-v0.1.0
git push origin plan-2-stage-m-complete plan-2-complete api-v0.1.0
```

- [ ] **Step 3：GitHub Release（非 draft；body 从 CHANGELOG 提取）**

```bash
gh release create api-v0.1.0 --title "api-v0.1.0" --notes-file <(
  awk '/## \[api-0.1.0\]/,/^## /' CHANGELOG.md | head -n -1
)
```

- [ ] **Step 4：commit + PROGRESS 更新**

```bash
git add CHANGELOG.md RELEASE.md README.md
git commit -m "docs: Plan 2 complete — api-v0.1.0 released

- CHANGELOG [api-0.1.0]: Stages A-M, 46 tasks, new pkg ruisheng-api
- RELEASE.md: api rollback runbook (4 scenarios) + smoke test
- README: Plan 2 [x] with links to quickstart"
```

master 更新 `docs/superpowers/plans/PROGRESS.md` 打 `Plan 2 完整闭环 ✅`。

---

## Self-Review（plan 写完的自查）

**Spec coverage 核对**（对应 §2.1 路由 + §3.1-3.8 数据流 + §5 错误/可靠性）：

| spec 位置 | 计划覆盖 | 备注 |
|---|---|---|
| §2.1 auth.py | C1 + C2 + C3 | 登录/注册/SMS/刷新/登出/OTP |
| §2.1 devices.py | D1 + D2 | CRUD + realtime + history 降采样 |
| §2.1 points.py | E1 | CRUD + update_flag |
| §2.1 alarms.py | E2 | 配置 + 记录 + reset |
| §2.1 reports.py | H1 | daily 聚合 + xlsx |
| §2.1 waveforms.py | H2 | 查询 + FFT |
| §2.1 plans.py | I1 + I2 | timing + maintenance |
| §2.1 scenes.py | I3 | pages + views |
| §2.1 pay.py | L1 + L2 | 下单 + notify |
| §2.1 orgs.py | G1 + G2 | 用户/联系方式/wx_groups |
| §2.1 ws.py | F3 | /ws endpoint |
| §2.1 services/notification | J1-J3 | INotifier + 3 适配器 + fanout |
| §2.1 services/wechat_pay | L1 | sign/verify |
| §2.1 services/analytics/fft | H2 | compute_fft |
| §2.1 tasks/* | K1-K3 | token/pay_expire/vacuum |
| §2.1 pubsub/publisher | D3 | XADD control |
| §2.1 pubsub/subscriber | F1 + F2 | alarm consumer + realtime bridge |
| §3.2 control 全流程 | D3 + D4 | XADD + 取消 |
| §3.5 wxpay 验签 | L2 | 严格顺序 + gw_pool |
| §3.6 RBAC 4×14 | B4 + 各 router | role + CA 位掩码 |
| §3.7 multi-tenant | B3 + 每 endpoint | SET LOCAL + RLS |
| §3.8.4 WS 慢消费者 | F1 | Queue(500) + drop-oldest |
| §3.8.8 history 降采样 | D2 | pick_sample_interval + X-Downsampled |
| §3.8.11 Outbox | 延后 gw 侧（Plan 1 F3 已覆盖） |  |
| §3.8.12 cmd_id 去重 | D3 | Idempotency-Key + 5s dup check |
| §3.8.17 maintain 状态机 | I2 | FOR UPDATE + GREATEST(now, old) |
| §5.1 ErrCode | A4 | 全套 handler |
| §5.13 JWT 加固 | B2 + C1 + C3 | typ/fp/jti blacklist |
| §5.13 登录锁 | B4 + C1 | 分布式 Redis |
| §5.12.3 脱敏 | B1 | patcher |

**Placeholder scan**：每个 Step 均含实际代码或具体命令，无 "TODO/实现 later"；部分 G2/I1/I3/J2/K2/K3/M1 标"参考 xx 模板"——已在对应模板 Task 中给出完整代码，复用模板不算 placeholder（但 implementer 须按模板节奏展开 Step 1-4）。

**Type 一致性**：
- `ApiResponse.code/msg/data/transid` 贯穿
- `CurrentUser(user_name, usr_group, role, control_authority, jti, fp)` 不变
- `apply_tenant_context(session, *, usr_group, role)` 所有调用一致
- `xadd_control_cmd(r, *, cmd_id, payload)` 一致
- repo 函数命名：`load_by_X`/`get_by_X`/`list_X`/`create_X`/`update_X`/`soft_delete_X`

**风险 & 未覆盖**（implementer 须知）：
1. **G2/J2/K2/K3/M1/I1/I3 简写**：这几个 task 沿用前序模板但 Step 代码未全量列出。implementer 执行时必须按本 plan 前置 Task 的"Files/Step 1-4"结构展开；若对 spec 字段有歧义 → BLOCKED 上报。
2. **alembic 迁移无新增**：Plan 0 已建全部 26 表；如 implementer 发现 spec §4.2 与 ORM 有差 → BLOCKED 上报、master 修 ORM 不在本 worktree 静默改。
3. **integration tests**：大多数 Task 只写 unit test（mock repo）。真连 PG/Redis 的 integration test 在 Stage M（tenant-lint + release 前）补齐端到端 smoke。
4. **openapi-typescript 生成**：前端用 openapi 自动生成 TS，Plan 3 web 前置任务，不在 Plan 2 内。
5. **CI 覆盖率 gate**：Plan 1 用 line 85% + branch 75%；api 保持同标（`fail_under=85` 继承）。若 api 某模块未达 → Stage M 前 implementer 补测试，不降标。
6. **Stage 顺序硬依赖**：A→B→C→D→(E,F 并行)→G→H→I→J→K→L→M。F 依赖 E（消费告警 → DB 查 user_alarm_list），不可颠倒。
7. **Windows 运行注意**：uvicorn `loop='asyncio'` 默认即可；`asyncio.Queue` 行为与 Linux 一致；integration test CI 在 Linux。本地 Windows 开发可用 fakeredis + 真 PG docker。

---

## 执行完毕交付清单（Stage M 后）

- [ ] tags：`plan-2-stage-{a,b,c,d,e,f,g,h,i,j,k,l,m}-complete` + `plan-2-complete` + `api-v0.1.0`
- [ ] GitHub Release `api-v0.1.0`（非 draft，body 自 CHANGELOG）
- [ ] PROGRESS.md：`Plan 2 — 完整闭环 ✅`
- [ ] memory `project_ruisheng_scada.md`：Plan 2 complete 记录 + 下一步 Plan 3 web
- [ ] `uv run pytest` 全绿（api-unit + api-integration 两 job 都 pass）
- [ ] `ruff check . && mypy .` clean
- [ ] line cov ≥ 85% / branch cov ≥ 75%（含 api）

**Plan 2 后的下一步**：Plan 3 ruisheng-web（Vue 3 + Vite + Pinia + ECharts + vue-konva，前端 SPA）。Plan 3 需新建 worktree `feature/plan-3-web`，从 `feature/plan-0-foundation` 派生（不从 plan-2-api，避免前端 openapi-typescript 生成时拉大量 Python 依赖）。
